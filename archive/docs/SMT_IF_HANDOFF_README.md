# 《真·女神转生 If...》PS1 中文化工程交接文档

> 最后更新：2026-07-20  
> 工程位置：`P:\ROMHacking\SMT IF`  
> 当前状态：**主线文本导入、F13/F14 字库生成、可执行文件补丁、光盘注入和 EDC/ECC 重建均已实现；普通剧情基本可运行。项目尚未完成，当前主要阻塞是部分 SLPM 内嵌菜单文本经过旧动态字库缓存路径时出现纹理/缓存错乱。**

---

## 1. 给下一位接手者的最短摘要

如果只看一页，请先看这里：

1. 前任“流星”已经完成了绝大多数文本导出、翻译和逆向笔记，全部保存在 `PreWork\if备案`。
2. 本轮工作没有继续维护前任中文变量名的单文件 `打包.py`，而是在 `new` 下重写成 JSON 数据、多模块 Python 和统一的 `build.py`。
3. 当前 `new\data\text.json` 有 12,367 条记录，三个分区均有译文：
   - `texts`：12,092
   - `text_17`：266
   - `text_88`：9
4. 当前码表 `new\data\codetable.json` 有 2,463 个连续字位，索引为 `0x0000..0x099E`。码值在 JSON 中按游戏实际的小端双字节写法保存，例如索引 `0x0001` 的键是 `"0100"`。
5. 动态字体 F13 已改成 1bpp、每字 24 字节、16×12 存储（实际绘制宽约 12）；原始 F13 解压容量正好是 `0xACE × 24 = 66,384` 字节。
6. 静态字体 F14 是 208×252 的 4bpp 四平面资源，只能直接容纳原始 `0x567` 个字位；高位中文必须通过低位 alias 方案映射进去。
7. 普通剧情动态字库解码器位于 `0x8005C218`；部分菜单/系统文本走另一套旧动态解码器 `0x8004ACB8`。第二套路径是当前主要问题所在。
8. F13、文本、压缩和镜像注入已经经过多轮验证。当前菜单乱码**不是** JSON 编码、码表、F13 文件、1bpp 解码或首次 VRAM 上传错误；这些环节都有运行时字节证据。
9. 当前最新诊断镜像是：

   `Game\modified\SMT_IF_CN_dual_f13_palette23_test.cue`

   SHA-256：

   `736BEC68347B2F74B5E21EAA2D1F6ED5CFCC0A01FD248A5C16F8216A8D39DEF9`

10. **不要把它当成发布候选版。** 它是为了调查第二套 F13 解码器和缓存而制作的实验镜像。
11. Git 工作树非常脏，大量关键代码仍未提交。不要执行 `git reset --hard`、`git checkout -- .` 或类似操作。
12. 下一步应从一次完全重启的干净运行开始，不读取受污染的即时存档，也不要再手工清零单个缓存表项。具体步骤见第 16 节。

---

## 2. 工程来源与目录分工

### 2.1 根目录

```text
P:\ROMHacking\SMT IF
├─ PreWork\               前任流星留下的逆向、Excel、码表和旧打包脚本
├─ new\                   本轮重写的 JSON/Python/ASM 构建系统
├─ extrac\                从原版镜像提取的文件与 ISO manifest
├─ Game\ogd\             干净原版镜像
├─ Game\modified\        构建结果和大量诊断镜像
├─ ram_vram\             调试期间保存的 RAM/VRAM dump 和截图
├─ saves\                 即时存档和记忆卡
└─ README.md              旧的三行说明，不足以交接
```

### 2.2 `PreWork\if备案`

这是前任工作的原始资料，**不要删除**。重要文件：

| 文件 | 作用 |
|---|---|
| `翻译.xlsx` | 主体翻译数据，约 900 KB |
| `if系统文本.xlsx` | 系统文本 |
| `原始码表.txt` | 原始码表 |
| `导入码表.tbl` | 前任准备的导入码表 |
| `批量导出文本.py` | 文本导出 |
| `偏移导出.PY` | 偏移导出 |
| `解包.py` / `解压解包.py` | 旧解包流程 |
| `打包.py` | 旧的一体化导入脚本，中文变量较多 |
| `CN.asm` | 原始的主动态字库 1bpp 补丁 |
| `四重字库.asm` | 大量字体/渲染逆向资料 |
| `动态字库.txt` | F13 动态字体路径的反汇编记录 |
| `4色字库文本读取.txt` | 静态/4bpp 字库相关记录 |
| `汉化笔记.txt` | F0001/F0012/F0013/F0014/F0016 等文件用途速记 |
| `armips.exe` | 当前构建仍在使用的 MIPS 汇编器 |

前任笔记对关键资源的判断：

| 文件 | 前任判断 / 当前理解 |
|---|---|
| F0001 | debug 文本？ |
| F0003 | 单色字库 |
| F0012 | GBA4bpp 静态字库；与命名/UI 路径有关，当前构建不修改 |
| F0013 | VB2bpp 动态字库，当前已改成 1bpp |
| F0014 | 4bpp 四平面静态字库，窗口宽 104 等路径使用 |
| F0016–F0018 | 压缩文本 |
| F0021–F0023 | 压缩文本 |
| F0025–F0038 | 压缩文本 |
| F0040/F0043 | 文本 |
| F0063 | 未压缩文本？ |
| F0073 | 字库？ |
| F0075–F0077 | 文本 |
| F0082 | 输入法 |
| F0083 | 片头图像 |
| F0084 | 文本 |
| F0088 | 未压缩系统文本，即当前 `text_88` |
| F0094 | 文本 |
| F0098 | 未压缩文本、自定义编码 |

### 2.3 `extrac`

`extrac\D\Fxxxx.BIN` 是原版光盘文件的提取结果。根目录还有：

- `SLPM_871.54`：原版可执行文件，1,081,344 字节。
- `_iso_manifest.json`：每个文件的 LBA、长度和提取方式。新构建依据它原位替换文件。
- `ZZZ.BIN` 等其他光盘文件。

**不要直接修改 `extrac` 中的基准文件。** `build.py` 总是从这里读取干净数据。

### 2.4 `new`

这里是当前应继续维护的实现：

```text
new
├─ build.py               统一构建入口
├─ data\
│  ├─ codetable.json
│  ├─ text.json
│  ├─ slpm_text.json
│  └─ original_ui_codetable.json
├─ src\                   分模块实现
└─ build\                 中间产物；不是源码
```

---

## 3. 环境和依赖

### 3.1 必需文件

- 干净原版镜像：

  `P:\ROMHacking\SMT IF\Game\ogd\Shin Megami Tensei If... (Japan).bin`

- 原版镜像大小：256,417,392 字节。
- 原版镜像 SHA-256：

  `03B286EC683F71AF968AF6A917D926F7F1C2A27BD1A6F3DF8D71086DFA79BCBF`

- 字体文件：

  `P:\ROMHacking\fusion-pixel-12px.otf`

- armips：

  `P:\ROMHacking\SMT IF\PreWork\if备案\armips.exe`

### 3.2 Python 依赖

核心构建需要：

```text
Python 3
Pillow
```

如果还要重新从 Excel 转 JSON，则需要：

```text
pandas
openpyxl
```

建议安装：

```powershell
python -m pip install Pillow pandas openpyxl
```

当前机器的系统 `Python 3.14` 环境不一定已安装 Pillow；之前的产物也曾由另一个 Python 3.12 环境生成。接手者应先确认自己实际调用的 Python 和依赖。

---

## 4. 构建入口和参数

工作目录：

```powershell
Set-Location 'P:\ROMHacking\SMT IF\new'
```

### 4.1 只验证资产，不生成镜像

```powershell
python build.py --dry-run --unified-normal-text
```

### 4.2 生成当前推荐路线的测试镜像

不要直接覆盖已有 `SMT_IF_CN_build.bin`，建议另取文件名：

```powershell
python build.py `
  --unified-normal-text `
  --output '..\Game\modified\SMT_IF_CN_handoff_test.bin' `
  --cue '..\Game\modified\SMT_IF_CN_handoff_test.cue' `
  --force
```

### 4.3 参数含义

| 参数 | 含义 | 状态 |
|---|---|---|
| `--dry-run` | 构建所有资源但不生成整张镜像 | 推荐先跑 |
| `--force` | 允许覆盖指定输出镜像 | 谨慎使用 |
| `--skip-base-hash` | 跳过原版镜像 hash 检查 | 不推荐 |
| `--static-aliases` | 旧的 F14 高字位 alias 实验路线 | 实验/已被统一路线替代 |
| `--relocate-dynamic-low-codes` | 把命名/UI 占用的低位字复制到高位供普通文本使用 | 实验 |
| `--unified-normal-text` | 综合处理普通文本、F14 静态文本、低位 UI 保留和 SLPM 内嵌文本 | 当前推荐路线 |

`--unified-normal-text` 不能与另外两个旧 alias/relocation 参数组合，代码会主动报错。

### 4.4 一个容易踩坑的事实

当前 `build.py` 只有在启用 `--unified-normal-text` 时才会执行 `slpm_text.json` 的内嵌可执行文件文本补丁。默认无参数构建虽然能生成镜像，但不会导入这些 SLPM 系统文本，因此不能把默认构建当成当前完整路线。

### 4.5 构建做的五件事

`build_assets()` 的顺序：

1. 用码表和 override 生成 F13/F14 所需的 1bpp 字体和预览图。
2. 重建 F0013 动态字体资源。
3. 重建 F0014 静态字体资源。
4. 用 armips 修改 `SLPM_871.54`，再写入静态宽度表和内嵌文本。
5. 编码 `text.json`，重建所有涉及的文本文件。

之后 `build_disc()`：

1. 校验原版镜像 hash。
2. 复制干净镜像到 `.tmp`。
3. 按 manifest 原位替换所有文件。
4. 为每个修改的 Mode 2 Form 1 扇区重建 EDC/ECC。
5. 回读验证。
6. 原子替换输出文件并生成 `.cue`。

---

## 5. 数据文件格式

### 5.1 `codetable.json`

格式：

```json
{
  "0000": "某",
  "0100": "字"
}
```

键是游戏中实际写入的两字节小端序。代码把它转换为：

```python
index = int.from_bytes(bytes.fromhex(code), "little")
```

因此：

- `"0100"` → 索引 `0x0001`
- `"AF01"` → 索引 `0x01AF`
- `"F008"` → 索引 `0x08F0`

当前码表：

- 2,463 个连续字位。
- 最低 `0x0000`。
- 最高 `0x099E`。
- 最初统计翻译约使用 2,200 个不同字符；随后为了兼容旧码表、系统文本和保留 UI 字符，最终码表比这个数字大。

### 5.2 `text.json`

根节点分为：

```text
texts      普通压缩文本
text_17    F0017 静态/特殊文本
text_88    F0088 未压缩系统文本
```

典型记录：

```json
{
  "id": "F0016-000079D8-000002C2",
  "block_offset": "000079D8",
  "pointer_offset": "000002C2",
  "source": "日文▽",
  "translation": "中文▽"
}
```

字段含义：

- `block_offset`：压缩资源在对应 `Fxxxx.BIN` 文件中的起始偏移，不是文本起点。
- `pointer_offset`：原始指针表里指向的、解压后资源内部的文本偏移。
- `id` 必须与这两个字段完全一致，构建器会校验。
- `translation` 非空时使用译文；为空时回退到 `source`。

当前所有 12,367 条 `text.json` 记录都有非空译文，但这不代表翻译质量已经 QA，也不代表 SLPM 内嵌文本全翻完。

### 5.3 `slpm_text.json`

这是后来发现并整理出的可执行文件内嵌系统文本。根节点必须只有：

```json
{
  "text_slpm": []
}
```

记录示例：

```json
{
  "id": "SLPM-000E6722",
  "offset": "000E6722",
  "max_bytes": 26,
  "source": "ＣＯＭＰを持っていない▽",
  "translation": "没有ＣＯＭＰ▽",
  "translation_origin": "manual",
  "renderer": "dynamic"
}
```

含义：

- `offset` 是 `SLPM_871.54` 文件偏移，不是 RAM 地址。
- `max_bytes` 是固定槽长度，必须是偶数。
- 编码后不能超槽。
- 剩余空间用 `FF` 填充。
- `renderer` 为 `dynamic` 或 `static`，决定使用哪套码表 override。

当前统计：

- 总记录：286
- dynamic：237
- static：49
- 已有译文：178
- 未翻译：108
- 来源：manual 120、existing_exact_match 58、untranslated 108

所以即使解决当前乱码，仍有 108 条 SLPM 系统文本需要翻译和测试。

### 5.4 `original_ui_codetable.json`

命名输入和一字节 UI 路径占用一批低编号。当前明确保留的索引包括：

```text
0x004, 0x005, 0x006, 0x007, 0x00D, 0x010, 0x015
0x02A..0x04D
0x068..0x108
```

`original_ui_glyphs.py` 会把这些低位 F13 字形强制恢复成原版 UI 字符，避免普通中文字体覆盖命名界面依赖的字位。普通中文文本如需这些字符，由 unified/relocation 计划移动到高位副本。

---

## 6. 文本控制码

`src\text_codec.py` 的控制码如下：

| 文本标记 | 字节 |
|---|---|
| `{00FF}` | `00 FF` |
| `▽` | `01 FF` |
| `※※※※※※※` | `02 FF` |
| 换行 | `03 FF` |
| `{主角}` | `04 FF` |
| `{队友}` | `05 FF` |
| `{名字1}` | `18 FF` |
| `{名字4}` | `19 FF` |
| `{道具}` | `1B FF` |
| `{种族1}` | `67 FF` |
| `{种族2}` | `68 FF` |
| `{主角等人}` | `6F FF` |
| `{仲魔}` | `70 FF` |
| `{数值0}` | `71 FF` |
| `{72FF}` | `72 FF` |
| `{73FF}` | `73 FF` |
| `{数值1}` | `76 FF` |
| `{名字3}` | `77 FF` |
| `{种族3}` | `78 FF` |
| `{恶魔1}` | `7A FF` |
| `{魔法}` | `7B FF` |
| `/` | `7C FF` |
| `{名字2}` | `7D FF` |
| `{恶魔2}` | `7F FF` |
| `{数量}` | `88 FF` |
| `{大停顿}` | `92 FF` |
| 全角空格 | `FE FF` |
| 文本结束 | `FF FF`，编码器自动追加 |

`encode_text()` 是单次从左到右扫描，不使用全局正则替换。它会：

- 优先识别 `{...}` 控制符。
- 特判 `▽`、七个 `※`、换行、`/` 和全角空格。
- 普通字符从反向码表查两字节编码。
- 拒绝未知控制符、孤立 `}` 和码表缺字。
- 最后自动添加 `FFFF`。

不要把控制码加入普通字库统计，它们不是字形索引。

---

## 7. 文本资源重建方式

### 7.1 记录分组

普通记录按以下键分组：

```text
(section, file_name, block_offset)
```

每个资源重建成：

```text
新的 4 字节指针表
+ 去重后的编码文本
```

共享同一个旧文本偏移的记录必须编码成相同内容，否则报错。

### 7.2 `text_88`

`text_88` 不是普通压缩资源。它的记录被视为一张连续的 4 字节指针表，构建器要求指针槽从最小 `block_offset` 开始每次加 4，随后直接写回原文件。

### 7.3 F0018 特例

这是剧情黑屏排查中最重要的文本特例：

```python
PASSTHROUGH_RESOURCE_KEYS = {
    ("F0018", 0x1F5C),
}
```

替换 F0018 的 `0x1F5C` 资源会导致地图/剧情加载卡死，即使保留原始文本偏移和长度也会出问题。当前构建对这一整个资源保持原样。

另有：

```python
EXACT_SIZE_RECORD_IDS = {
    "F0018-00001F5C-00000358"
}
```

原因是这条记录还被正常指针表之外的顺序读取逻辑使用，结束符必须处在原始结尾；不能在较早的 `FFFF` 后随意填充空隙。

这项处理解决了“命名后第一段剧情结束黑屏/卡死”的主要文本资源问题。不要重新启用该资源替换，除非已经逆向出它的非文本引用。

---

## 8. 压缩格式

游戏常用 Atlus 风格资源头。

### 8.1 LZ77

```text
01 02 [resource id 2字节]
[整个压缩块长度 uint32 LE，包含12字节头]
[解压后长度 uint32 LE]
[token stream]
```

当 resource id 为零时，常见头就是：

```text
01 02 00 00
xx xx xx xx
xx xx xx xx
```

token：

- `control < 0x80`：literal，长度 `control + 1`，后跟原文。
- `control >= 0x80`：回溯，长度 `control - 0x7D`，下一字节 `distance - 1`。
- 最大 literal：128。
- 最大 match：130。
- 回看窗口：256 字节。
- 允许重叠复制；另有 conservative non-overlap 编码器供诊断。

### 8.2 RLE

```text
01 01 [resource id]
[总长度]
[解压长度]
[token stream]
```

token 结构与 LZ77 类似，但高 token 的第二字节是重复值，而不是回看距离。

### 8.3 未压缩资源

```text
01 00 [resource id]
[总长度，包含8字节头]
[raw data]
```

### 8.4 已经验证的结论

- 用当前压缩器把原版 F13 解压后重新压缩并插回，游戏可以正常运行。
- 所以早期 F13 黑屏不是“我们的 LZ77 算法必然不兼容”。
- 当前文本资源会在 LZ77 和 raw 之间选择更小者，并做解压回环验证。
- F13 需要额外保持原始主压缩块的声明长度，当前通过把部分 back-reference 重新 token 化为 literal，把流扩展到精确长度。

---

## 9. F13 动态字库

### 9.1 原始结构

原始 `F0013.BIN`：

- 文件总长：61,440 (`0xF000`)。
- 主资源声明长度：60,056 (`0xEA98`)。
- 主资源解压长度：66,384 (`0x10350`)。
- 这正好等于 `0xACE × 24`。
- 主资源后仍有 1,384 字节尾部，不能视为无意义 padding。
- 尾部开头还有第二个 LZ77 资源：压缩长度 22，解压长度 32，内容符合 16 色 PS1 CLUT。

尾部第二资源开头：

```text
01 02 00 10 16 00 00 00 20 00 00 00 ...
```

### 9.2 当前字体格式

- 单字存储：16×12 = 192 bit = 24 字节。
- 实际中文绘制区宽度约 12 像素，额外列用于纹理/对齐。
- bit0 是每字节最左像素。
- `0` 是字形笔画，`1` 是透明背景，即反相 1bpp。
- 字体来源：`P:\ROMHacking\fusion-pixel-12px.otf`，12px。

`font_builder.py` 生成：

- `font_f13_1bpp.bin`
- `font_f13_preview.png`
- `font_f14_1bpp.bin`
- `font_f14_preview.png`

### 9.3 F13 重建必须遵守的规则

`font_resource.py` 当前会：

1. 解压原始 F13 主资源。
2. 用新字体覆盖开头的字位。
3. 未覆盖的高位保留原始数据，不能清零。
4. 重新 LZ77 压缩并回环验证。
5. 把压缩流扩展到原主资源的精确声明长度 `0xEA98`。
6. 原样保留整个尾部，包括 CLUT 子资源。
7. 保持 F0013 文件总长度 61,440 不变。

早期黑屏的重要教训：**不要只写新压缩流后把剩余 F13 全部填零，也不要丢弃 `0xEA98` 后的尾部。**

### 9.4 两套动态解码器

普通剧情路径：

```text
0x8005C218
```

它已经由前任 ASM 改成每次读 24 字节 1bpp 并展开。动态文字宽度在：

```text
0x8005B2E4 -> 12
```

另一套菜单/系统旧路径：

```text
0x8004ACB8
```

原版这里仍按每字 48 字节的 2bpp F13 读取。只改第一套解码器时，普通剧情正常，但 COMP/MAGIC、SLPM 菜单提示等会把相邻字形数据组合成点阵乱码。

当前 `CN.asm` 已重写第二套路径：

```text
source = [0x8010B4D0] + glyph_index × 24
读取24字节
输出24个word = 96字节4bpp
背景 nibble 3，笔画 nibble 2
```

该路径的输入、输出和 VRAM 上传都已经验证正确，当前问题发生在之后的缓存/绘制阶段，详见第 14、15 节。

---

## 10. F14 静态四平面字库

F14 不是 F13 的另一份简单字节数组。

当前确认：

- 原文件：24,576 字节。
- 主图形资源声明长度：23,541。
- 解压图像尺寸：208×252。
- 4bpp。
- 四个 bit plane。
- 每 plane 346 个槽。
- 总静态槽：`0x567 = 1,383`。
- 单元格：12×12。
- 每列 21 个单元格。

字位布局：

```text
plane = index // 346
slot  = index % 346
x = (slot // 21) × 12
y = (slot % 21) × 12
```

`static_font_resource.py` 会把前 `0x567` 个 1bpp 字形分别写入四个 plane 的对应 bit，同时保留 F14 尾部 CLUT/padding。

静态字宽表：

```text
RAM/SLPM address: 0x800F2910
```

当前构建把 `0x000..0x566` 全部设为 `0x0B`，用于解决中文静态文字间距过近的问题。

因为 F14 只能直接容纳低 `0x567` 个字，高位中文必须映射到未使用的低槽。`static_text_aliases.py` 和 `unified_normal_text_plan.py` 正是为此存在。

---

## 11. F12、命名界面与低编号冲突

命名界面不是普通剧情渲染器。它至少包含：

- 原版日文输入法/键盘图像。
- 一字节或低编号字符路径。
- 与 F12/F13/F14 不完全相同的 UI 资源组合。

项目曾多次出现：

- 普通中文对话正确，但 `NAME:` 和输入内容乱码。
- 名字在后续剧情里正常，但命名阶段显示错误。
- 修命名低位字后，普通对话中的“声”“多”等字错位。
- COMP/MAGIC 文字和中文使用不同颜色/渲染器。

最终得出的原则：

1. 命名键盘不必汉化，可以保留原版日文。
2. 不能让普通中文文本直接依赖命名/UI 的特殊低位编号。
3. 低位 UI 字形应保留原版；普通文本需要这些字符时复制到高位。
4. 静态 F14 文本如需高位中文，再单独 alias 到安全低槽。

三条实验路线：

- `static_text_aliases.py`：最早的 F14 alias，可能搬动普通文本低位字符。
- `dynamic_low_code_relocation.py`：把特殊低位字符的普通文本副本搬到 F13 高位。
- `unified_normal_text_plan.py`：综合统计所有普通/静态/SLPM 文本后统一分配，当前推荐。

当前仍不能宣称命名界面已经最终完成。测试时要分别检查：

- `NAME:` 标题。
- 键盘假名。
- 实时输入的名字。
- 剧情里的 `{名字...}` 控制符。
- 菜单角色名。

---

## 12. ASM 和可执行文件补丁

源码：

`new\src\CN.asm`

当前补丁：

| 地址 | 作用 |
|---|---|
| `0x80049578` | 缓存指针表容量由 `0x567` 改为 `0xACE` |
| `0x8005B2E4` | 普通动态字宽固定为 12 |
| `0x8005C1D0` / `0x8005C218...` | 主动态 F13 1bpp 解码器 |
| `0x8004ACB8...` | 第二套旧动态 F13 1bpp 解码器，当前实验补丁 |

`executable_patch.py`：

1. 校验原版 SLPM SHA-256：

   `8F06D6B81DE1BAC70D3658424AFC635C42F8AC3B2F727CAF6BF88D99B4B31CF4`

2. 把原版复制到 `new\build\armips`。
3. 去掉前任 ASM 中误留的 ARM `.pool`。
4. 调 armips。
5. 验证关键 opcode。
6. 验证 ASM 恰好修改 303 个字节。
7. 写静态宽度表 override。
8. 写 SLPM 固定槽文本。
9. 保持 SLPM 总长度 1,081,344 不变。

不要在已经补丁过的 SLPM 上再次叠加 armips；脚本始终应从 `extrac\SLPM_871.54` 开始。

---

## 13. 光盘写入和 EDC/ECC

镜像是 raw BIN：

- 每扇区 2,352 字节。
- Mode 2 Form 1 用户数据从偏移 24 开始。
- 每扇区用户数据 2,048 字节。

`disc_injector.py` 的限制：

- 替换文件必须与 manifest 中原文件长度完全一致。
- 不进行 ISO 文件重排或扩容。
- 替换前验证原扇区已有 EDC/ECC。
- 替换后回读文件验证字节完全一致。

EDC：

```text
输入：sector[0x10:0x818]
输出：sector[0x818:0x81C]，uint32 little-endian
```

ECC：

1. 暂存 `0x0C:0x10` 的地址/模式字节。
2. 计算 Mode 2 ECC 时把这四字节临时清零。
3. P parity 写 `0x81C:0x8C8`。
4. Q parity 写 `0x8C8:0x930`。
5. 恢复地址/模式字节。

因此当前构建已经自动处理 EDC/ECC，不需要再用外部工具二次修复。

---

## 14. 从开始到现在的排查时间线

### 阶段 A：熟悉前任资料

- 确认前任翻译和逆向基本完成，主要缺导入和运行时适配。
- 区分 F12/F13/F14：不是同一份字库的三个副本。
- 决定放弃继续扩展中文变量名的 `打包.py`，改成多文件 Python + `build.py`。
- 把 Excel 转成 JSON，以 `id + block_offset + pointer_offset` 保存记录。

### 阶段 B：码表和字体

- 从翻译统计约 2,200 个不同字符。
- 用前任导入码表的编号建立 JSON 码表。
- 保留原有字位顺序，再在末尾补缺字。
- 把 P1 的字体构建器改成 IF 的 16×12、24 字节 1bpp。
- 修正字体 bit polarity：背景为 1、笔画为 0。

### 阶段 C：F13 压缩和最早的黑屏

- 整理前任 LZ77 类，补上解压、roundtrip 和多种诊断编码器。
- 从原版镜像复制干净副本，只替换 F13。
- 一度在 Atlus 动画后黑屏，无模拟器报错。
- ASM 单独应用不黑屏，确定问题由 F13 文件触发。
- 观察到状态字节 `0x801076A8` 与 attract mode/开场循环有关，在 `0x80027E18` 一带能看到状态写入；但后来确认这是坏资源引起的症状，不是根因。
- 用当前压缩器把原版 F13 重压再插回，游戏完全正常，排除 LZ77 基本算法。
- 最终发现 F13 主资源后还有 CLUT 子资源/尾部，并且某些加载路径依赖主资源原始声明长度。
- 当前 `font_resource.py` 通过精确长度和完整尾部保留解决这一类黑屏。

### 阶段 D：主文本导入

- 实现 `encode_text()` 和控制码。
- 实现记录编码、指针表重建、文本去重、资源压缩、文件回写。
- 实现光盘注入和 EDC/ECC。
- 普通剧情开始能够显示中文。

### 阶段 E：命名后剧情黑屏

- 命名结束后第一段剧情曾卡死/黑屏。
- 对 F0018 资源做多轮二分测试。
- 定位到 F0018 `0x1F5C` 资源及 `pointer_offset 0x358` 的特殊读取。
- 当前把整个资源设为 passthrough，剧情恢复。

### 阶段 F：F14 和系统文本

- 重新生成 F14 四平面字库。
- 修改静态宽度表，解决中文间距过近。
- 引入 alias/relocation/unified 方案，避免 F14 高位不可见和命名低位冲突。
- 一度做到普通剧情、选项和大量系统文字正常；剩余明显问题集中到命名和某些可执行文件内嵌菜单文本。

### 阶段 G：SLPM 内嵌文本

- 从 `if系统文本.xlsx` 和运行截图中整理 `slpm_text.json`。
- 已定位并翻译诸如：
  - “没有可用魔法”
  - “没有ＣＯＭＰ”
  - 菜单功能说明
  - 记忆卡/插槽提示
- 内嵌字节写入验证正确，但显示为随机中文、方框、图标或人物纹理碎片。

### 阶段 H：发现第二套动态 F13 解码器

- 断点确认 SLPM 菜单文本经过：

  ```text
  0x8009A85C
  -> 0x8009A880 / 0x8009A8B8
  -> 0x8004AC94
  -> 0x80049300
  -> renderer 0x800495B0
  ```

- 发现实际字形生成使用 `0x8004ACB8`，而不是普通剧情的 `0x8005C218`。
- 给 `0x8004ACB8` 增加 24 字节 1bpp 解码补丁。
- 同时把缓存指针表容量从 `0x567` 扩成 `0xACE`，避免高编号直接越过表尾。

### 阶段 I：当前缓存/绘制问题

- 文本、F13 源数据、解码输出、缓存节点、上传矩形和首次 VRAM 内容全部验证正确。
- 但屏幕仍出现正确汉字、灰块、图标、错误纹理和 COMP 混合。
- 手工把单个高位 table entry 清零会改变现象，但会制造重复节点和链表/表不一致，不能当成修复。
- 当前必须从干净重启继续观察缓存生命周期或绘图 primitive。

---

## 15. 当前乱码问题的完整证据链

### 15.1 出问题的示例

SLPM 固定槽：

```json
{
  "id": "SLPM-000E670E",
  "offset": "000E670E",
  "max_bytes": 20,
  "source": "使える魔法が無い▽",
  "translation": "没有可用魔法▽",
  "renderer": "dynamic"
}
```

```json
{
  "id": "SLPM-000E6722",
  "offset": "000E6722",
  "max_bytes": 26,
  "source": "ＣＯＭＰを持っていない▽",
  "translation": "没有ＣＯＭＰ▽",
  "renderer": "dynamic"
}
```

### 15.2 可执行文件槽字节正确

`0xE6722` 的可见编码：

```text
F0 08  -> 08F0 没
09 09  -> 0909 有
AF 01  -> 01AF Ｃ
B0 01  -> 01B0 Ｏ
A2 01  -> 01A2 Ｍ
B1 01  -> 01B1 Ｐ
01 FF  -> ▽
FF FF  -> 结束
```

因此 JSON、码表、槽偏移、长度、`▽` 和 `FFFF` 都正确。

### 15.3 运行时文本缓冲正确

曾在运行缓冲看到：

```text
08F0 0909 01AF 01B0 01A2 01B1 FF01 FF03 FF03 FFFF
```

额外 `FF03` 来自内嵌文本经过两次 preprocess；高 FF 项按控制码处理，不是随机字形。运行缓冲中没有那些屏幕上看到的随机汉字/图标。

### 15.4 F13 源地址正确

全局 F13 基址：

```text
[0x8010B4D0] = 0x801C4400
```

计算：

```text
source = 0x801C4400 + code × 0x18
```

例：

```text
Ｃ 01AF -> 0x801C6C68
有 0909 -> 0x801D1CD8
```

`有` 的运行时 24 字节：

```text
EF FF 00 F8 F7 FF FB FF 03 FC F9 FD
02 FC FB FD 03 FC FB FD 7B FC FF FF
```

与 `font_f13_1bpp.bin` 对应字位完全一致。

### 15.5 解码输出正确

`没（08F0）` 某次输出缓冲：

```text
0x80139EF8
```

输出 96 字节与离线转换逐字节一致。开头：

```text
23 33 23 22 32 33 33 33
33 32 23 33 32 33 33 33
33 33 23 33 32 33 33 33
...
```

### 15.6 缓存节点正确

同一次 `没` 的节点：

```text
s0 = 0x8014D2C0
E0 30 F0 08 01 00 01 00 ...
```

解析：

```text
U = 0xE0
V = 0x30
code = 0x08F0
```

### 15.7 上传矩形正确

调用点：

```text
0x80049980 -> 0x800E2948
```

像素源：

```text
s5 = 0x80139EF8
```

RECT：

```text
38 02 30 01 04 00 0C 00
```

即：

```text
VRAM X = 0x238 = 568
VRAM Y = 0x130 = 304
W = 4 VRAM words = 16 个 4bpp 像素
H = 12
```

坐标公式：

```text
X = 0x200 + U / 4
Y = 0x100 + V
```

### 15.8 首次上传后的 VRAM 正确

VRAM 是 1024×512、每 word 2 字节。线性字节偏移：

```text
((Y × 1024) + X) × 2
```

第一行：

```text
((304 × 1024) + 568) × 2 = 0x98470
```

由于 VRAM 行跨度为 `0x800` 字节：

```text
0x98470: 23 33 23 22 32 33 33 33
0x98C70: 33 32 23 33 32 33 33 33
0x99470: 33 33 23 33 32 33 33 33
```

这些字节均与解码输出一致。

### 15.9 由此已经排除

当前乱码不是以下原因：

- JSON 译文写错。
- 字符编码错。
- `▽`/`FFFF` 漏写。
- SLPM 固定槽偏移错。
- F13 文件高位字形为空。
- `code × 24` 算错。
- 1bpp 解码循环算错。
- 输出缓冲指针错。
- 首次 LoadImage 的矩形宽高错。
- 首次 VRAM 上传没有发生。
- LZ77 压缩器普遍不兼容。

### 15.10 尚未决定的两个方向

只剩：

1. 正确字形首次上传后，在真正显示前被后续纹理上传覆盖，且缓存映射没有失效；或
2. VRAM 内容仍正确，但后续绘制 primitive 使用了错误/过期的节点、UV、TPage 或 CLUT。

由于改变玩家姓名会改变乱码内容，而且曾出现人物动画纹理碎片，缓存/VRAM 生命周期仍是高优先级怀疑；但尚不能称为 GPU 异步竞态“实锤”。

---

## 16. 动态缓存结构与下一步调试

### 16.1 全局结构

当前缓存全局结构基址：

```text
0x8010B4D0
```

已知字段：

| 偏移 | 含义 |
|---|---|
| `+0x00` | F13 字体基址，运行时为 `0x801C4400` |
| `+0x04` | 字形/表数量，当前设为 `0xACE` |
| `+0x0C/+0x10` | free list 尾/头一类字段 |
| `+0x14/+0x18` | reusable/LRU list 头/尾 |
| `+0x1C` | `code -> node*` 指针表 |

初始化函数 `0x8004AFE0`：

- 分配 `0x930` 字节节点池。
- 节点大小 `0x10`。
- 共 `0x93 = 147` 个节点。
- 分配 `count × 4` 的 code 指针表并清零。

147 个 VRAM 格子布局：

- U：`0x80..0xE0`，步长 `0x10`，7 列。
- V：`0x00..0xF0`，步长 `0x0C`，21 行。
- `7 × 21 = 147`。

所以不能只扩大 RAM 节点池；还必须安排新的、不冲突的 VRAM 格子。

### 16.2 节点布局

每节点 16 字节：

| 偏移 | 大小 | 含义 |
|---|---:|---|
| `+0` | byte | U |
| `+1` | byte | V |
| `+2` | halfword | 字形 code |
| `+4` | byte | 状态，观察到 0/1/2 |
| `+6` | halfword | 引用计数；注意“+6”是字段偏移，不是加 6 |
| `+8` | word | next |
| `+C` | word | prev |

### 16.3 关键函数

| 地址 | 作用 |
|---|---|
| `0x8004B0EC` | 从 free list 取节点 |
| `0x8004B128` | 从 reusable/LRU list 取节点 |
| `0x8004B168` | 命中可复用节点时把它从链表摘除 |
| `0x8004B20C` | 重置引用并把节点接入另一条链表；需要继续确认调用方式 |

这说明原程序并不是直接无视引用计数随机淘汰节点。若要证明生命周期 bug，应检查链表是否被破坏、节点是否过早进入 reusable list，而不是只假设 147 个槽必然不够。

### 16.4 查找/创建流程

| 地址 | 行为 |
|---|---|
| `0x80049824` | `lhu code, 0(s1)` |
| `0x8004982C` | `code × 4` |
| `0x80049830` | 加 table base |
| `0x80049834` | `lw s0, table[code]` |
| `0x8004983C` | `s0 == 0` 时走 miss |
| `0x80049864` | 读取 node+6 引用计数 |
| `0x80049870` | 引用计数 +1 后写回 |
| `0x80049880` | 把 node 指针写进本次文本的节点列表 |
| `0x80049888` | 先从 free list 分配 |
| `0x8004989C` | free 为空时从 reusable/LRU 分配 |
| `0x800498B8` | 淘汰旧节点时清 `table[old_code]` |
| `0x800498FC` | 写 `table[new_code] = node` |
| `0x80049940` | 调 `0x8004ACB8` 解码字形 |
| `0x80049974` | 调 `0x800E269C`，形态很像 `DrawSync(0)`，名称尚未正式确认 |
| `0x80049980` | 调 `0x800E2948` 上传字形 |

### 16.5 绝对不要再做的诊断操作

不要在 `0x8004983C` 已经读出 `s0` 后，只把 `table[code]` 手工清零并继续长期运行。

原因：

1. 旧节点仍带同一个 code，并留在链表中。
2. 新 miss 又为同一 code 创建第二个节点。
3. 将来淘汰旧节点时，`0x800498B8` 会执行 `table[old_code] = 0`。
4. 它可能把新节点的有效表项一起清掉。
5. 之后会出现重复节点、频繁 miss、灰块、图标和正确字混合，画面每次不同。

本轮调试后期出现的“没/有正确但中间夹灰块和图标”就是在这种受污染状态下观察到的，不能作为干净游戏行为。

### 16.6 下一位接手者应执行的干净测试

1. 完整关闭并重启模拟器。
2. 使用最新实验镜像，不加载本轮受污染的即时存档。
3. 不修改任何 RAM、寄存器或 cache table。
4. 在 `0x80049834` 设置执行断点。
5. 触发第一次“没有ＣＯＭＰ”。
6. 每次停住检查 `[s1]`；直到第一次看到：

   ```text
   F0 08 = 08F0 = 没
   ```

7. 此时记录：
   - `v0`（`table[08F0]` 的地址）
   - `[v0]` 的四字节

判断：

- 如果这是冷启动后第一次出现 `08F0`，但 `[v0] != 0`：高编号 table 初始化/清理不完整，直接追踪是谁写入了这个表项。
- 如果 `[v0] == 0`：让它自然 miss，不做手工修改；在 `0x80049980` 记录新节点 `s0`、像素源 `s5` 和 RECT。

然后让错误画面真正显示，暂停，再比较：

1. 节点前四字节是否仍为原来的 `U V code`。
2. 同一 VRAM 坐标的字节是否仍与首次上传一致。

结果解释：

- VRAM 变了、节点没变：后续 LoadImage 覆盖了仍被缓存引用的格子。对 `0x800E2948` 做条件日志，找相同 RECT 的下一次写入。
- 节点 code/U/V 变了：节点在绘图消费前被复用或列表持有过期指针。
- VRAM 和节点都没变：转查消费节点列表的绘图 primitive，重点是 UV、TPage、CLUT 和 primitive 数量。

---

## 17. 重要测试镜像

`Game\modified` 有大量二分/诊断镜像。不要逐个当作候选发布版。下面是最有参考价值的几个：

| 镜像 | SHA-256 | 用途/已知表现 |
|---|---|---|
| `SMT_IF_CN_build.bin` | `E0656DD1CF2EE7F0B1EE26C06634E329943D7E506507A380B11171B6BFACC594` | 2026-07-20 12:17 的普通 build 输出，不含后来第二套 F13 诊断补丁 |
| `SMT_IF_CN_F14_alias_width_test.bin` | `BCAC5C00CBD07599564AB91C710D1983FB0CFCBBCB1E797E5E53DA910D4124C5` | 早期 F14 alias/宽度参考点；普通文本仍有低位冲突，NAME/COMP 也未解决 |
| `SMT_IF_CN_unified_fixed_test.bin` | `4F67EAFD308D8A5838D993F3D3F41D04BAF3A7CECCFF9C325B8B3AF808D2C6DA` | 普通剧情错字明显减少、曾用于对照“多”等字符；命名字体表现不同 |
| `SMT_IF_CN_dual_f13_palette23_test.bin` | `736BEC68347B2F74B5E21EAA2D1F6ED5CFCC0A01FD248A5C16F8216A8D39DEF9` | 最新，含第二套 F13 解码器和 palette 2/3；当前缓存/绘制问题的基准 |

其他文件名大致反映实验内容：

- `F0013.*.BIN`：早期 F13 压缩、尾部、重叠 LZ77、长度二分。
- `SMT_IF_DIAG_F0018_*`：F0018 剧情卡死二分。
- `SMT_IF_CN_name_*`：命名页/低位字库实验。
- `SMT_IF_CN_diag_original_F13/F14`：恢复原字体的 renderer 对照。
- `SMT_IF_CN_diag_F13_indices`：直接显示 F13 编号诊断。
- `SMT_IF_CN_renderer_split_test` / `comp_static_test`：区分 static/dynamic renderer。
- `SMT_IF_CN_dual_f13_decoder_test` / `palette3` / `palette23`：第二套解码器与调色板实验。

---

## 18. RAM、VRAM 和存档资料

`ram_vram` 中的重要 dump：

| 文件 | 说明 |
|---|---|
| `init_black_scren.bin` / `_2` / `_3` | 早期 F13 黑屏 RAM |
| `init_normal.bin` | 正常启动 RAM 对照 |
| `init_black_scren_vram.bin` | 黑屏 VRAM |
| `init_normal_vra.bin` | 正常 VRAM |
| `naming_ram.bin` / `naming_vram.bin` | 命名界面正常/基准 |
| `after_name_corrupt_ram.bin` / `_vram.bin` | 命名后乱码状态 |
| `naming_wrong.bin/png` | 命名字库错误截图/VRAM |
| `8049988.bin/png` | 最新 `0x80049988` 上传后 VRAM dump/图像 |

`saves`：

- `SLPM-87154_resume.sav`
- `SLPM-87184_2.mcd`

注意即时存档会包含动态 cache、table 和 VRAM 状态。缓存调试必须区分：

- 干净冷启动。
- 正常记忆卡存档。
- 模拟器即时存档。

不要用受手工内存修改影响的即时存档证明初始化 bug。

---

## 19. 已经走过但不应重复的错误方向

1. **反复怀疑文本编码。** 当前“没有ＣＯＭＰ”的磁盘和运行缓冲编码已逐字节确认。
2. **把 F13 后半部当 padding 清零。** 它包含 CLUT 子资源和需要保留的尾部。
3. **认为所有 F13 黑屏都是 LZ77。** 原版数据经当前压缩器重压可正常运行。
4. **只替换 F13，不修改所有读取路径。** 至少有 `0x8005C218` 和 `0x8004ACB8` 两套动态解码器。
5. **把 F14 当线性 F13。** F14 是四平面 4bpp 图。
6. **直接覆盖命名/UI 的低码位。** 会导致 NAME、输入内容、普通对话和菜单互相破坏。
7. **把 `node+6` 看成“引用计数加6”。** `+6` 是字段偏移，实际值每次加1。
8. **认为字符集扩到 2766 就会同时占 2766 个 cache 槽。** 物理压力取决于同时活跃的不同字形；147 槽是否不足必须用运行证据判断。
9. **只扩大 0x930 节点 RAM。** 节点和 VRAM 格子一一对应，盲目扩大会制造坐标冲突。
10. **只 memset table，不重建节点链表。** 会留下孤儿节点和重复 code。
11. **把 `0x801076A8` 状态机当黑屏根因。** 它是坏资源影响游戏流程后的症状。
12. **重新启用 F0018 0x1F5C 文本。** 会重新引入剧情/地图卡死。

---

## 20. 当前代码模块索引

| 文件 | 作用 |
|---|---|
| `build.py` | 总入口，生成字体、文本、SLPM、镜像和 CUE |
| `src\codetable.py` | 旧码表转 JSON、字符统计 |
| `src\translation_io.py` | Excel → JSON；只在重新导入 Excel 时使用 |
| `src\text_codec.py` | 字符和控制码编码 |
| `src\text_records.py` | JSON 校验、记录选择和编码 |
| `src\text_block_builder.py` | 指针表/文本块重建 |
| `src\text_resource_builder.py` | 压缩资源读取、打包和特殊布局 |
| `src\text_file_builder.py` | 把重建资源写回 Fxxxx 文件 |
| `src\text_pipeline.py` | 串联所有文本步骤 |
| `src\compression.py` | LZ77/RLE/raw 编解码和精确长度扩展 |
| `src\font_builder.py` | 16×12 1bpp 字体生成和预览 |
| `src\font_resource.py` | F13 重建，保留尾部和精确长度 |
| `src\graphic_resource.py` | Atlus 图形 RLE、4bpp pack/unpack |
| `src\static_font_resource.py` | F14 四平面重建 |
| `src\static_text_aliases.py` | 旧 F14 alias 方案和静态宽度表 |
| `src\dynamic_low_code_relocation.py` | UI 低位普通文本搬迁方案 |
| `src\unified_normal_text_plan.py` | 当前统一字位分配方案 |
| `src\original_ui_glyphs.py` | 恢复保留低位的原版 UI 字形 |
| `src\slpm_text.py` | SLPM 内嵌文本槽校验和编码 |
| `src\executable_patch.py` | armips、宽度表、SLPM 固定槽补丁 |
| `src\disc_injector.py` | raw sector 注入、EDC/ECC、回读验证 |
| `src\CN.asm` | 当前 MIPS 补丁 |

---

## 21. Git 状态和保存建议

当前仓库只有很早的三个提交：

```text
2745c23 update readme
79abf7f update readme
3204051 codetable change complete
```

大量当前工作未提交。已修改/新增内容包括：

- `new\src\CN.asm`
- `new\src\compression.py`
- `new\src\disc_injector.py`
- `new\src\executable_patch.py`
- `new\src\font_builder.py`
- `new\src\font_resource.py`
- 几乎整个新文本 pipeline
- `new\build.py`
- `new\data\codetable.json`
- `new\data\text.json`
- `new\data\slpm_text.json`
- `new\data\original_ui_codetable.json`

还有一个已删除的旧文件：

```text
new\src\text_injection.py
```

接手者第一件事应该是：

1. 对整个目录做外部备份。
2. 查看 `git diff` 和 untracked 文件。
3. 把当前状态提交到新的 handoff 分支或归档，而不是直接清理工作树。
4. 把 `__pycache__`、`new\build` 和大镜像按需加入忽略规则，但不要误删唯一的诊断镜像。

---

## 22. 建议的后续优先级

### P0：保存和可复现

- 保存当前脏工作树。
- 安装 Pillow 并跑 `--dry-run --unified-normal-text`。
- 用单独输出名生成镜像，确认当前源码能复现最新资源组合。
- 记录输出 SHA-256、armips 输出和文本构建统计。

### P0：解决 legacy dynamic cache/绘制问题

- 按第 16.6 节从冷启动观察第一次 `08F0` lookup。
- 不做单表项手工清零。
- 比较首次上传和错误画面显示后的相同 VRAM 格子。
- 若被覆盖，记录所有命中同一 RECT 的 `0x800E2948` 调用和节点状态变化。
- 若未覆盖，找到 `0x80049880` 写入的节点指针列表的消费函数，检查生成的 primitive 数量、UV、TPage、CLUT。
- 检查场景/字体重载时是否有另一套只清理 `0x567` 项的路径。静态扫描还发现 `0x80047430` 和 `0x80078268` 一带存在 `0x567` 立即数，但它们看起来属于其他结构，尚未完全证明相关。

### P1：命名界面

- 保持日文键盘原样是可接受目标。
- 验证 NAME 标题、实时输入、剧情名、菜单名使用的分别是哪套字体/编码。
- 不要再通过全局换低位字形解决局部问题。

### P1：剩余翻译

- 翻译 `slpm_text.json` 中 108 条空译文。
- 对菜单功能说明、战斗设置、反射/抗性说明、记忆卡、插槽进行逐项 QA。
- 搜索仍留在图形或未扫描区域的日文。

### P1：完整流程测试

- 冷启动/开场循环。
- 新游戏与命名。
- 第一段剧情到可自由移动。
- COMP/MAGIC/ITEM/EQUIP/STATUS/CHANGE。
- 记忆卡读取、保存、无存档、插槽1/2。
- 战斗、商店、队伍、仲魔、魔法、道具。
- 吸引模式第二轮，避免早期 `0x801076A8` 类回归。

### P2：整理工程

- 添加 `requirements.txt`。
- 为 compression、text codec、F13/F14 roundtrip 建自动测试。
- 把实验镜像移到单独 archive，并维护 `experiments.md`。
- 给 ASM 地址和缓存结构补正式符号。

---

## 23. 发布前检查表

发布候选必须同时满足：

- [ ] 从 hash 正确的干净原版镜像构建。
- [ ] 构建无 `--skip-base-hash`。
- [ ] 所有替换文件长度与 manifest 相同。
- [ ] 每个修改扇区 EDC/ECC 校验通过。
- [ ] F13 主块解压为 66,384 字节。
- [ ] F13 `0xEA98` 后尾部与原版一致。
- [ ] F14 主资源和尾部校验通过。
- [ ] 普通剧情无错字/挤字。
- [ ] 静态系统文本无高位缺字。
- [ ] 命名界面可用。
- [ ] COMP/MAGIC 等 legacy dynamic 文本无灰块、图标和随机纹理。
- [ ] F0018 第一段剧情不卡死。
- [ ] 保存/读取记忆卡正常。
- [ ] 开场和 attract mode 多轮循环正常。
- [ ] 完整游戏流程至少一次实机级模拟器测试。
- [ ] 输出 BIN/CUE 的 SHA-256 已记录。

---

## 24. 最后的判断

这个项目并不是“前面的所有工作都失败了”。相反，最难的大部分基础设施已经完成：

- 文本已经结构化并可重建。
- 字库可以自动生成。
- F13/F14 格式已经理解并可回环验证。
- ASM 可以自动应用并校验 opcode。
- 光盘可以安全原位注入并重建 EDC/ECC。
- 普通剧情和大量系统文本已经能够显示中文。

真正没有完成的是多套历史渲染器共用同一套低位字位、动态缓存和 VRAM 资源时的最后兼容层，尤其是 `0x8004ACB8` / `0x80049824` 这条 legacy dynamic 路径。

接手者最应避免的是再次从 Excel、码表、LZ77 或 EDC/ECC 推倒重来。当前证据已经把主要问题推进到：

```text
正确字形已经进入 VRAM
→ 显示前后缓存节点/VRAM 是否变化
→ 绘图 primitive 是否引用正确的纹理页和调色板
```

从这一点继续，才是离最终完成最近的路线。
