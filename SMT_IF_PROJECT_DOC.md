# 《真·女神转生 If...》PS1 中文化 —— 项目技术文档

> 更新：2026-07-23
> 工程位置：`P:\ROMHacking\SMT IF`
> 本文档取代 `SMT_IF_HANDOFF_README.md`（旧交接文档；其第 15/16 节的"GPU 缓存竞态"方向已被证伪，勿再沿用）。
> 第四阶段（07-22~23）的破案与修复见 §9.4 / 附录A 第四阶段。

---

## 0. 项目状态总览

| 部分 | 状态 |
|---|---|
| 主线剧情对话（压缩文本文件 F0016/F0018…） | ✅ 已导入并**实机确认显示中文**（含 F0018 副本坑修复） |
| SLPM 内嵌系统文本（菜单标签/地名/六维） | ✅ 全译且正常 |
| SLPM 战斗系统消息块（0xE4A00–0xEF400） | ⏳ 已提取 33 条（陷阱/异常/道具失效/卡隆死亡界面/地图标记），**待翻译** |
| F0049 商店 + 回复精灵菜单（overlay） | ⏳ 已完整提取 233 条，用户已译 ~75，**其余待翻译** |
| F0092 事件/剧情flag/图鉴/击破记录（overlay） | ⏳ 已完整提取 788 条，**待翻译** |
| F0098 种族/物品类别表 | ✅ 全译 |
| 恶魔名/技能名 | ✅ 已翻译（text.json） |
| 装备栏物品名（F0094） | ✅ 已修（缺字：入 F14 静态集，见 §9.4） |
| 六维缩写 | ✅ 力**智**魔体速运（知→智 已改） |
| 名字复数后缀 達→**等** | ✅ 已改（"由美等"） |
| **卡死（守护灵/获取仲魔/交涉/回复点等）** | ✅ **根治**（exact-size 注入，见 §9.4） |
| 命名界面 / 键盘 | ✅ 可用（键盘保留日文=设计决定） |
| 全流程 QA + 翻译回填 | ⏳ 进行中 |

最新可测镜像：`Game/modified/SMT_IF_CN_test11.bin/.cue`（= test10，含全部 bug 修复）。
安全回退镜像：`test9`（紧凑注入版，万一 exact-size 出问题）。
（构建可复现：任何时刻用 `build.py --unified-normal-text` 从干净原版 `Game/ogd/` 重新生成。）

⚠️ **两大陷阱（排查必记）**：
1. `Game/modified/Shin Megami Tensei If... (Japan).bin` **不是原盘**（是旧构建）！真原盘 = `Game/ogd/…(Japan).bin`（== extrac 解包源）。拿错参照会得出完全错误的 diff 结论。
2. 对话/事件文本在文件内是**LZ77 压缩资源**，直接在镜像里搜明文搜不到 ≠ 没有；判断已翻/未翻必须先按资源解压或用 RAM dump 反查。

⚠️ **Git 工作树未提交**——强烈建议尽快 `git add` + commit（这轮改了 build.py / src/text_file_builder.py / src/text_resource_builder.py / src/overlay_text.py / src/executable_patch.py / tools/rearrange_codetable.py + 多个 data/*.json）。

---

## 1. 目录结构

```text
P:\ROMHacking\SMT IF
├─ PreWork\                前任"流星"的资料（勿删）
│  ├─ if备案\
│  │  ├─ 翻译.xlsx             旧版翻译（已被新版取代）
│  │  ├─ if系统文本.xlsx       text_88 来源
│  │  ├─ 原始码表.txt          ★原版 编号→日文 对照（UTF-16LE，XXXX=字）
│  │  ├─ 导入码表.tbl          前任的导入码表（codetable.json 的最初来源）
│  │  └─ armips.exe            MIPS 汇编器（构建仍在用）
│  └─ 真女if (1).xlsx          ★当前生效的新版翻译（含校对列、名词表）
├─ extrac\                 原版光盘提取物（★只读基准，勿改）
│  ├─ SLPM_871.54              原版可执行文件（1,081,344 字节）
│  ├─ D\Fxxxx.BIN              原版数据文件
│  └─ _iso_manifest.json       每个文件的 LBA/长度（原位注入依据）
├─ Game\ogd\               干净原版镜像（sha 03B286EC…）
├─ Game\modified\          构建输出与测试镜像（*_test 可定期清理，每个 256MB）
├─ ram_vram\               调试期 RAM/VRAM dump（多为旧构建产物，仅考古用）
└─ new\                    ★当前实现
   ├─ build.py                 构建总入口
   ├─ data\                    所有翻译/码表数据（JSON）
   ├─ src\                     分模块实现
   └─ tools\                   textscan.py / rearrange_codetable.py
```

---

## 2. 快速上手：构建与验证

```powershell
Set-Location 'P:\ROMHacking\SMT IF\new'

# 只验证资产（不生成 256MB 镜像）
python build.py --dry-run --unified-normal-text

# 生成完整镜像
python build.py --unified-normal-text `
  --output '..\Game\modified\SMT_IF_CN_xxx.bin' `
  --cue    '..\Game\modified\SMT_IF_CN_xxx.cue' --force
```

- 依赖：Python 3（系统 3.14 可用）+ Pillow；重新导 Excel 需 openpyxl。
- `--unified-normal-text` 是当前唯一正路（旧 `--static-aliases` / `--relocate-dynamic-low-codes` 已废弃）。
- **改过码表后必须重新构建全部**（字库+文本+SLPM 是一体的）。
- 测试**必须冷启动 + 新游戏**：旧即时存档/记忆卡里的名字是按旧码表编码的，码表变过之后读旧档必然乱码，这不是 bug。

构建五步（`build_assets`）：字体渲染 → F0013 重建 → F0014 重建 → SLPM 补丁（armips+宽度表+内嵌文本+F0098 注入）→ 文本文件重建；然后 `build_disc` 原位注入 + EDC/ECC 重建 + 回读校验。

---

## 3. 核心概念：一个字是怎么显示出来的

### 3.1 两字节编码与码表

游戏文本 = 一串**两字节小端字库码**（不是 Shift-JIS！），`FFFF` 结尾，高字节 `FF` 的是控制码（`01FF`=▽、`03FF`=换行、`18FF`={名字1}…完整表见 `src/text_codec.py`）。

`data/codetable.json`：键=小端十六进制（`"1901"`→编号 0x0119），值=字。当前 2469 字，编号连续 0x0000–0x09A4。**必须连续**（`load_codetable` 拒绝空洞）。

### 3.2 F13 动态字库（对话/菜单动态文字）

- `F0013.BIN`，LZ77 压缩，解压 66,384 字节 = 0xACE × 24。
- 每字 24 字节 1bpp、16×12 存储（实际画 12 宽）；bit0=最左像素；**0=笔画、1=透明**。
- 运行时整体解压到 RAM（基址存 `[0x8010B4D0]`，通常 0x801C4400），`字形地址 = 基址 + 码 × 24`。
- 重建规则（`font_resource.py`）：保留主块声明长度 0xEA98、保留尾部 1384 字节（内含菜单 CLUT 子资源，见 §9-3）。

### 3.3 F14 静态字库（菜单静态文字/名字面板）

- `F0014.BIN`：208×252、4bpp、**四个位平面**，每平面 346 格 → 共 0x567=1383 格，单元 12×12。
- 布局：`plane=idx//346; slot=idx%346; x=(slot//21)*12; y=(slot%21)*12`。
- **容量只有 0x567** ⇒ 一切"会被静态路径画的字"必须放在码表低位（<0x567）——这是码表重排的核心约束。
- 静态字宽表在 SLPM `0x800F2910`（1 字节/字），构建统一写 0x0B。

### 3.4 原版小字库与 Shift-JIS（队伍名字的旧路径）

游戏里 **Shift-JIS 只出现在两处**：F0063 的开发调试文本（裸 SJIS，不用管）和**队伍面板名字的原版渲染链**：

```text
名字（16位字库码）→ 0x8006C27C 调 0x80046FEC
  → 内部查表把码转成 Shift-JIS → 用 SJIS 索引一张预制小字纹理（~8px）绘制
```

这张纹理表（`[[0x80107378]+0x2448]`，16 bank×32 格）只覆盖 <0x200、实际填到 0x108（原版命名字符集），中文码一律落到共用回退图块——这就是"由美显示 .."的根因。

**现行方案**：`0x8006C27C` 改为 `jal 0x800475FC`（同参数约定，直接按 16 位码读 F14 绘制）。配套要求：**所有可能出现在队伍面板的名字用字必须 <0x567**（重排工具已保证）。代价：名字 12px，比原版小字大（§12）。

### 3.5 渲染路径总表

| 路径 | 字库 | 解码器/入口 | 覆盖内容 |
|---|---|---|---|
| 对话动态 | F13 | `0x8005C218`（已改 1bpp） | 剧情对话 |
| legacy 菜单动态 | F13 | `0x8004ACB8`（已改 1bpp）+ 渲染器 `0x800495B0` + 147 格 VRAM 缓存 | COMP/MAGIC 等菜单动态文字 |
| 静态 | F14 | `0x800475FC` 一族 | 菜单静态标签、战斗固定文字 |
| 队伍名字 | ~~小字库(SJIS)~~ → F14 | `0x8006C27C → jal 0x800475FC` | 队伍面板成员名 |

两条动态路径**共用同一张字形缓存**（结构基址 `0x8010B4D0`：+0 字库基址、+4 容量(0xACE)、+0x1C 码→节点表；节点 16B：U,V,code,状态,引用计数,链表指针；147 个 VRAM 格 U=0x80..0xE0 V=0x00..0xF0）。

---

## 4. 码表布局约束（`tools/rearrange_codetable.py`）

改动用字集合（翻译变化/加字）后运行：`python tools/rearrange_codetable.py`，然后重新构建。它固化了以下**血泪换来的规则**：

1. **连续无空洞**，索引 0..N-1。
2. **index 0 = 全角空格**：原版 0 号是空字形，命名界面按空格插 0 号；`font_builder` 另外强制 0 号全透明。
3. **硬编码码位钉死**：状态屏六维缩写表（SLPM `0xE5C20`）直接存字码 ⇒ `力=0x53B 知=0x3CD 魔=0x4D2 体=0x3A8 速=0x39A 运=0x180` 永远钉在原位。其中最后一项由构建器从原版 `運=0x131` 补丁为简体 `运=0x180`。以后发现新的"UI 硬编码字码"，同样加进 PINS。
4. **静态/F14 集合必须 <0x567**：text_17、text_88、renderer=static 的 SLPM 槽、F0098 表、**队伍可见名字**（预设名+恶魔名译文，脚本自动从 text.json 抓 F0040 片假名源记录）。
5. 其余动态字填满非保留码位后，溢出的落入**保留码位**（命名界面专用段 0x004,5,6,7,0xD,0x10,0x15 + 0x02A–0x04D + 0x068–0x108），由构建时 `unified_normal_text_plan` 自动**重定位**到高位副本（当前溢出 192、目的池约 300，够用；报 "Need X destinations" 就是不够了）。

保留码位的字形由 `original_ui_glyphs.py` 恢复成原版 UI 字（假名等），**F13 和 F14 都要加**（equip 名字曾因 F14 漏加而乱码）。

---

## 5. 数据文件（`new/data/`）

| 文件 | 内容 | 注入通道 |
|---|---|---|
| `text.json` | texts(12,261)/text_17(266)/text_88(9)；`id=文件-块偏移-指针偏移` | 文本管线（§6） |
| `slpm_text.json` | 347 条 SLPM 内嵌固定槽；`offset`=SLPM 文件偏移、`max_bytes`=槽长(偶数)、`renderer`=dynamic/static（**决定用哪套码，别丢这个字段**） | `executable_patch` 写槽，FF 填充 |
| `f0098_text.json` | 85 条种族/物品类别；offset=F0098.BIN 文件内偏移 | `src/f0098_text.py` 裸表注入（写前校验槽与原版一致） |
| `overlay_text.json` | **事件 overlay 内嵌文本**：F0049(233,商店+回复菜单)、F0092(788,事件/图鉴/击破)。格式 `{id,file_offset,byte_len,max_bytes,source,translation}` | `src/overlay_text.py` 原地等长注入（未压缩代码 overlay，直读 file_offset；约束 编码+FFFF≤byte_len） |
| `codetable.json` | 编号→字 | 全局 |
| `original_ui_codetable.json` | 保留码位→原版 UI 字 | 字库渲染 override |
| `untranslated_names*.json` | 名字翻译工作清单（已完成，留档） |
| `*.bak` | 各文件重排/导入前备份 |

**translation 为空 ⇒ 回退显示日文原文**（`choose_text`）。Excel 导入注意：空单元格经 pandas 会变字符串 `"nan"`——历史上 195 条"nan 假翻译"曾骗过覆盖率检查。

### 新版 xlsx 导入要点（`PreWork/真女if (1).xlsx`）

- sheet `全文本` 有表头行（旧版没有）；列 4=译文、列 5=校对，**优先取校对**。
- F0017 的行要拆到 text_17；新版没有 F0088（沿用旧 text_88）。
- 系统性错误要清洗：`{72FF}}`→`{72FF}`（404 处）、`{数值}`→`{数值0}`。
- sheet `名词表`：人名/地名官方译名（由美/玲子/明/查理/龙一/晓子/亚蒙/法阶/封印之间…）。

---

## 6. 文本管线与特例

- 普通记录按 `(section, file, block_offset)` 分组 → 重建 4 字节指针表 + 去重文本 → LZ77/raw 取小 → 回环验证。
- 压缩格式（Atlus 头 `01 02` LZ77 / `01 01` RLE / `01 00` raw）：literal ≤128、match ≤130、窗口 256、允许重叠。
- **★资源注入 = exact-size 偏移稳定**（§9.4 #16）：`_rebuild_resource_runs` 优先让每个被替换资源保持**原始偏移+原始声明大小**(`_to_exact_size`)，同时满足游戏的"固定偏移直读"和"顺序读"两种读法；仅当某资源译文重压后超出原大小时，该 run 才退回紧凑重排（只对顺序读的对话文件安全）。**这是卡死类崩溃的根治，勿改回无条件紧凑。**
- **INPLACE_RESOURCE_KEYS**（F0018 0x1F5C、F0040 四块）：需要**资源内部字符串偏移也固定**的资源（游戏按固定内部偏移读串），走 `build_inplace_resource_patches` 固定布局+等大小。exact-size 只固定资源文件偏移(level-1)，这个固定内部串偏移(level-2)。
- text_88 是连续指针表直写，不走压缩。
- 恶魔名/技能名就在 texts 的 F0040 记录里（裸表位于翻译后的资源内），无需单独注入。

---

## 7. 可执行文件补丁（`src/CN.asm`）

| 地址 | 改动 | 目的 |
|---|---|---|
| `0x80049578` | 0x567→0xACE | 动态缓存"码→节点表"容量（越界=花屏噪点） |
| `0x8005B2E4` | lbu→li 12 | 对话字宽固定（高码越界读垃圾宽度） |
| `0x8004A064` | lbu→li 12 | **菜单**字宽固定（同一张 0x567 宽度表 `0x800F2E80` 的第二处读取；漏改曾致标签拉成白条+叠字） |
| `0x8005C1D0/218` | 重写 | 对话解码器：24B 1bpp（输出 底=4/笔画=0） |
| `0x8004ACB8` | 重写 | legacy 菜单解码器：24B 1bpp（输出 底=3/笔画=2） |
| `0x8006C27C` | jal 0x80046FEC→0x800475FC | 队伍名字改走 F14（弃 SJIS 小字库） |

**executable_patch.py 里的直接字节补丁（非 CN.asm，仿 luck-stat 模式，都带原值断言）**：
| 文件偏移 | 改动 | 目的 |
|---|---|---|
| 0xE5C20 表 entry5 | 0x131(運)→0x180(运) | 六维 运（简体） |
| 0xE5C20 表 entry1 | 0x3CD(知)→智码位 | 六维 知→**智**（`resolve_static_ui_glyph` 构建时解析） |
| 0x1D6B0 immediate | 0x3BB(達)→0x1DB(等) | 名字复数后缀"由美**等**"（`addiu $a1,$zero,imm; sh $a1,($a0)`） |

`executable_patch.py` 校验：原版 SLPM sha、armips 改动字节数（**改 CN.asm 后记得同步这个断言**）、关键 opcode 表；随后写静态宽度表、SLPM 槽文本、上述硬编码字。**别在已补丁的 SLPM 上二次跑 armips**，永远从 `extrac/` 干净副本开始（脚本已保证）。硬编码 UI 字（智/等）用 `resolve_static_ui_glyph()` 构建时从码表解析（带 <0x567 断言），**重排后不会悄悄失效**。

另有两处 `0x567`（`0x80047430`、`0x80078268`）属于 F14 静态字库本身，**是正确的，勿改**。

菜单字库调色板：F0013 尾部 LZ77 子资源（文件偏移 0xEAA5 起的字面区）。已把 idx0 改成 0xE318（亮），解决"对话预缓存的字在菜单里发暗"。

---

## 8. 光盘注入

raw BIN 2352B/扇区，Mode2 Form1，用户数据偏移 24、长 2048。`disc_injector.py`：按 manifest 原位替换（长度必须一致，不重排不扩容）→ 每个改动扇区重建 EDC（`sector[0x10:0x818]`→`0x818`）与 ECC（P `0x81C:0x8C8`、Q `0x8C8:0x930`，计算时临时清零地址字节）→ 回读校验。**无需外部修复工具。**

---

## 9. Bug 编年史（症状 → 真根因 → 修法）

调试本项目的最大教训：**症状会说谎，dump 要新鲜**。三周的"GPU 缓存竞态"追凶，用的全是补丁前旧镜像的 dump。

1. **早期黑屏**：F13 重建把尾部当 padding 清零/改变主块声明长度 → 保留尾部+精确长度。
2. **命名后剧情卡死**：F0018 0x1F5C 有非指针表读取 → passthrough。
3. **菜单彩色噪点/纹理碎片、换名字就变**：码→节点表只有 0x567 项，高码查表越界拿到垃圾节点 → 0xACE。～"换名字就变"是因为不同码踩到不同垃圾内存。
4. **菜单文字拉成长白条+叠字**：菜单字宽读 `0x800F2E80[码]` 越界（没=129px）→ `0x8004A064` 固定 12。
5. **同一句里一半亮一半暗**：两解码器输出编码不同（对话 0/4、菜单 2/3）+共用缓存+菜单 CLUT idx0 是暗色 → 改 CLUT idx0 为亮。
6. **状态屏"武敢/听肚/男男男"**：不是渲染 bug，是 slpm_text 提取当年从 0xE602C 开始、**漏了前面 0xE5C44–0xE5DC2 的标签块**；"男男男"=空装备占位符 `－－－…`。→ 补译 36 条。（用原始码表.txt 解码原版字节找回原文。）
7. **equip 标签"声声/音击"**：equip 用 F14，标签字重排后 >0x567 不在 F14 → fallback 到 0 号字形 → 标签改 renderer=static + 重排钉低位。
8. **命名空格变"音"**：原版 0 号=空白槽，我们连续码表在 0 号放了字 → index0 钉空格+字形强制透明。
9. **六维变"脆孩布急享感"**：六维缩写表硬编码字码 → 力知魔体速钉回原码位，末项补丁为简体 `运=0x180`。
10. **由美显示".."**：SJIS 小字库路径不认中文码（§3.4）→ 改走 F14 + 恶魔名用字钉进 F14（不钉的话 361 个恶魔名 272 个会错）。
11. **"nan"假翻译**：Excel 空格→pandas→字符串"nan"，曾被当成已翻译。

诊断方法论（都验证有效）：
- DuckStation 存 RAM/VRAM dump → 离线逐字节读缓存结构/绘制 primitive/CLUT/槽字节；
- 用 `原始码表.txt` 解码原版数据、用当前码表+重定位表解码新数据；
- 从构建出的字库逐字渲染成图人工核对（`unpack_1bpp`）；
- 症状是**确定性**的就不是竞态。

### 9.4 第四阶段（07-22~23）：满屏乱码 + 卡死类根治

12. **"越玩越乱/满屏对话乱码"的真相**：主线对话游戏其实读的是 **F0018@0x1F5C 这一份**（F0016 里有翻译好的**重复副本**但游戏不读），而 F0018 那份被前人 passthrough 保留日文 → 字库重排后原版日文变乱码。**排查关键**：用 RAM dump，原版码表解出成句日文=原版残留，新码表解出垃圾。**修法**：`text_resource_builder.py` 的 **INPLACE_RESOURCE_KEYS**——等大小原地替换（固定布局+expand_lz77 补到原 total_size），文件其他字节零移动，避开前人踩的地图加载器卡死。
13. **达→滴 / 装备属性标签乱**：达是名字复数后缀，游戏硬编码 `addiu $a1,$zero,0x3BB` @SLPM 0x1D6B0 追加该码 → executable_patch 改 immediate 到"等"(0x1DB)。属性标签(攻击/防御/抗火炎…)其实早就用 slpm_text static 翻好了，旧截图是废版本。**通用套路**：游戏硬编码插入的字，改 SLPM immediate 到目标中文字现码位即可，不必动重排。
14. **知→智**：六维缩写表 entry1，executable_patch 把 0x3CD(知)→智码位（`resolve_static_ui_glyph` 构建时从码表解析，防重排失效）。
15. **装备栏物品名缺字（头/肩/轮…带缩进）**：物品名(F0094)用 F14 静态渲染，码≥0x567 的字渲染成空白但占位 → 重排把 item_name_texts 纳入静态集(<0x567)。**教训**：任何"某屏只显示名字的部分字+缩进"=F14 缺字，把那批名字加进 rearrange 静态集。
16. **★卡死类根治（PC=0x00020002）**：守护灵/获取仲魔/交涉/回复点等多处卡死同一根因——`_rebuild_resource_runs` 把 run 内资源**紧凑重排→文件偏移移位**，游戏按固定偏移读跳转表→读到字库码数据当指针→jr 飞进名字表数据区(RAM 0x20000)→hang。**资源两种读法**：(a)顺序读靠声明 total_size 找下一个→需紧凑；(b)固定偏移直读→不能移。**唯一同时满足=每个资源装回原版完全相同大小(exact-size)**。走过的弯路：先试"资源留原位、缩小补零到下一资源"→制造空隙破坏顺序读→**黑屏**（教训:补零留空隙会坏顺序读，必须 exact-size）。**最终修法**：`_to_exact_size`（解压→compress_lz77→expand 或"改header total_size+尾部填充，解压器读到 expected_size 就停"）；run 级混合：全装得进→整 run exact-size(布局逐字节同原版)、任一超出→退紧凑(仅顺序读的对话文件)。实测 1509 资源 exact-size、85 对话资源紧凑、0 损坏。
17. **F0049/F0092 提取严重不全**：F0049 不只是商店——是"服务 NPC 总 overlay"，含商店+**回复精灵菜单**(0x9868)+多 NPC 变体，横跨 0x0100 资源内外的裸代码区。旧提取器只扫单资源、且在 0xFFxx 标点断 run、且 `kana>=2` 漏掉单假名短选项(買う/売る)。**修法**：`overlay_text.py` 支持**直接文件偏移**(file_offset)，扫整文件、is_tc 含 (c>>8)==0xFF、过滤放宽到"文本区内无乱码≥2真字"。F0049→233 条、F0092→788 条（FFFF-split 交叉验证无遗漏）。

---

## 10. 工具（`new/tools/`）

**`textscan.py`** —— 文本侦探（找字符串在哪个文件哪个偏移）：
```text
python tools/textscan.py scan F0075 > out.txt   # 列出文件里所有可解码日文串
python tools/textscan.py find "ピクシー"         # 全文件搜一个日文词
python tools/textscan.py rev "慌脱"              # 屏幕错字→反查编号（错字考古第一步）
python tools/textscan.py slpm 0xE5C44            # 解码 SLPM 偏移
```
（控制台是 GBK 会乱码，重定向到文件看。）

**`rearrange_codetable.py`** —— 码表重排（§4）。改翻译/加字后：跑它 → `--dry-run` → 完整构建。

---

## 11. 常见工作流程

**A. 更新翻译文本**：改 `text.json`（或重新从 xlsx 导入，注意 §5 清洗规则）→ 若用字集合变了跑重排 → 构建。

**B. 加缺字**：`codetable.py` 的 `NEW_CODETABLE_ENTRIES` + `codetable.json` 末尾追加（连续编号）→ 跑重排 → 构建。字形从 `P:\ROMHacking\fusion-pixel-12px.otf` 自动渲染。

**C. 加/改 SLPM 系统文本**：`slpm_text.json` 加记录（offset/max_bytes/renderer 三者都要对；译文编码后≤槽长，超了就精简译文）。判断 renderer：走菜单静态画面的是 static。

**D. 诊断新的显示错误**：
1. 截图记下错字 → `textscan.py rev` 反查编号；
2. 编号 >0x567 且出现在静态画面 ⇒ F14 容量问题（该文本应算 static）；
3. 错字每次固定 ⇒ 多半是码位/表问题；随机变化 ⇒ 才考虑缓存/时序；
4. 必要时 DuckStation 存同帧 RAM+VRAM 给分析。

**E. 发布候选**：干净原版 hash 构建、无 `--skip-base-hash`、冷启动全流程测（开场→命名→第一段剧情→COMP/MAGIC/ITEM/EQUIP/STATUS/CHANGE→战斗→商店→仲魔→存读档→attract 二轮）、记录 SHA-256。

---

## 12. 已知限制与可选项

1. **队伍面板名字 12px**（原版 ~8px 小字）。位置正确、字偏大。要完全还原只有一条路：恶魔名保留假名（走原版小字库）+ 仅 3 个预设名扩小字纹理表（5 字塞得进 <0x200 的空格）；代价=恶魔名不翻译+中等逆向量（图集来源疑似 F0012，表构建在 `[[0x80107378]+0x2448]`）。当前判断：不值得，团队已翻恶魔名。
2. **命名键盘保留日文**（设计决定，输入假名在各处显示正常）。
3. F0063（调试文本）、F0082（输入法）、图形内嵌文字（标题画面等）未处理。
4. 磁盘：每个测试镜像 256MB，P 盘曾被塞满——定期清 `Game/modified/*_test*`。

---

## 13. 关键地址/文件速查

```text
原版镜像 sha256  03B286EC683F71AF968AF6A917D926F7F1C2A27BD1A6F3DF8D71086DFA79BCBF
原版 SLPM sha256 8F06D6B81DE1BAC70D3658424AFC635C42F8AC3B2F727CAF6BF88D99B4B31CF4
SLPM 装载基址    0x8000F800（文件偏移 = RAM 地址 − 0x8000F800）
动态缓存结构      0x8010B4D0（+4 容量；+0x1C 码→节点表）
菜单字宽表        0x800F2E80（0x567 项，1B/项；读取点已补丁绕过）
静态字宽表        0x800F2910（构建覆写为 0x0B）
六维缩写码表      SLPM 文件偏移 0xE5C20
菜单标签区        SLPM 0xE5C44–0xE5DC2；系统消息 0xE602C–0xE70FF；地名 0xE4768–0xE486A
F13 菜单 CLUT     F0013.BIN 尾部，字面区 0xEAA5（idx0 已改亮）
VRAM 字形格坐标   X=0x200+U/4, Y=0x100+V（4bpp）；VRAM 1024×512×2B
原版码表          PreWork/if备案/原始码表.txt（UTF-16LE）
```

---

## 14. 给下一个接手者的三句话

1. **先读本文档 §3/§4/§9**，旧交接文档的"缓存竞态"是歧路。
2. 任何显示乱码先问三个问题：**哪条渲染路径？字的码位在哪个范围？dump 是不是当前构建的？**
3. 改任何东西前：`git commit`、留 `.bak`、用 `--dry-run` 先验证。

---

## 附录A：项目历史 —— 谁做了什么

### 第一阶段：前任"流星"（`PreWork\if备案`，≤2025-09）

逆向与翻译的地基，本项目最大的单笔贡献：

- **全部文本导出**（批量导出文本.py / 偏移导出.PY / 解包.py），整理出 `翻译.xlsx`（约 12,000 条）并组织了翻译。
- **字库逆向**：识别 F0012/F0013/F0014 三套字库的性质与格式（四重字库.asm、动态字库.txt、4色字库文本读取.txt、汉化笔记.txt）。
- **原始码表**（原始码表.txt：原版 编号→日文 完整对照，本轮"错字考古"全靠它）与**导入码表**（导入码表.tbl，codetable.json 的前身）。
- **最初的 ASM 补丁**（PreWork 的 CN.asm，共 3 处）：对话字宽固定 12（0x8005B2E4）、字形步长 48→24（0x8005C1D0）、**对话解码器 1bpp 重写**（0x8005C218）——附录 B 的 #4/#5/#6 就是他的原作，沿用至今。
- 旧一体化导入脚本 打包.py（已被 new/ 取代，但思路被继承）。
- 局限：只改了对话一条渲染路径；导入流程是单文件脚本、不可回归验证。

### 第二阶段：本轮重写（`new\`，2026-07 中旬，用户 + Codex）

把"能跑的脚本"变成"可验证的构建系统"：

- **JSON 数据模型 + 模块化 Python + `build.py` 统一入口**（文本/码表/字库/补丁/光盘全链路，处处回环校验）。
- **F13/F14 重建**：吃透 LZ77/RLE 压缩、主块精确长度、尾部 CLUT 子资源保留、F14 四平面打包（早期黑屏的教训换来的，§9-1）。
- **光盘原位注入 + EDC/ECC 重建 + 回读校验**（disc_injector.py），从此不依赖外部修复工具。
- **发现并整理 SLPM 内嵌文本**（slpm_text.json），发现**第二套动态解码器 0x8004ACB8** 并写了 1bpp 补丁（附录 B #7），扩缓存表 0x567→0xACE（#1）。
- F0018 剧情卡死二分定位 → passthrough（§9-2）。
- 弯路：三周的"GPU 缓存竞态"追凶（用的是补丁前旧镜像的 dump），教训记在 §9 开头。

### 第三阶段：收尾冲刺（2026-07-20 ~ 07-21，用户 + Claude + Codex）

从"主线能玩但菜单全花"推进到"全部文本落地"：

- **证伪缓存竞态**，逐字节读 dump 定位真根因链：字宽表越界（#3，0x8004A064）、CLUT 明暗不一（F13 尾部 idx0）、漏提取的菜单标签块（36 条补译）。
- **名字体系**：用原始码表反查错字 → 定位并翻译 F0098 种族/物品表（新注入器 f0098_text.py）、SLPM 地名、恶魔名/技能名核对；6 个缺字补入码表。
- **新版翻译全量导入**（真女if (1).xlsx：+435 行、校对稿、名词表；清洗 `{72FF}}` 等系统性错误）。
- **码表重排**及其全部约束的发现与固化（§4，rearrange_codetable.py）：index0=空格、六维钉位、静态/F14<0x567、队伍名字用字钉低位。
- **队伍名字**：Codex 找到 SJIS 小字库瓶颈并做了 `jal 0x800475FC` 换渲染器补丁（附录 B #8）；Claude 审查后补齐恶魔名用字钉进 F14（不补则 361 名中 272 个会错）、去掉不统一的 10px 特殊字号。
- 工具（textscan.py）与本文档。

### 第四阶段：破案与卡死根治（2026-07-22 ~ 07-23，用户 + Claude）

起因：测试群反馈"玩到后面全是乱码"+多处卡死。逐一破案：

- **满屏对话乱码**：不是翻译没做，是 partynames 那次只改了字库没跑完整文本注入 + F0018@0x1F5C 副本坑（游戏读的副本被 passthrough 保留日文）。用 RAM/VRAM dump 反查坐实，建 INPLACE 等大小原地注入机制（§9.4 #12）。**排查中踩的坑**：一度拿 `Game/modified/…Japan.bin`（其实是旧构建）当原盘参照、又被文件内 LZ77 压缩误导——两点已写进 §0 警告。
- **A 类硬编码字**：達→等（SLPM immediate）、知→智（六维表）、装备物品名缺字（入 F14 静态集）。属性标签本就翻好了（旧截图是废版本误报）（§9.4 #13-15）。
- **★卡死类根治**：守护灵/获取仲魔/交涉/回复点等 PC=0x00020002 卡死 = 资源紧凑重排导致文件偏移移位、固定偏移读到坏指针。改为 exact-size 偏移稳定注入根治（§9.4 #16）。中途"补零留空隙"方案导致黑屏，已记教训。
- **漏翻补全**：F0049(233,商店+回复菜单)、F0092(788,事件/图鉴)完整重提取（旧提取器严重不全）；SLPM 战斗块 33 条提取（陷阱/异常/道具失效/卡隆死亡界面/地图标记）。全部 translation 待翻译组回填。
- **仍未解决**（见 `new/问题.txt`）：升级界面 のこりポイント 定位（疑似动态拼接）、これでよろしいですか 未生效（F0075 疑似副本）、恶魔交涉选项缺字、杰克霜精自动扣血是否真 bug。

---

## 附录B：CN.asm 逐条技术分析

`new/src/CN.asm` 共 8 处补丁。armips 以 `.org 地址` 直接覆写指令；`executable_patch.py` 会校验关键 opcode 和改动总字节数（改 asm 后记得同步断言）。以下按地址排列。

### #1 `0x80049578`：动态缓存表容量 0x567 → 0xACE

```asm
addiu   v0,$zero,0xACE
```
原指令 `addiu v0,zero,0x567`。这个值写入缓存结构 `0x8010B4D0+4`，初始化函数 `0x8004AFE0` 按 `容量×4` 分配"码→节点指针表"并清零。F13 从 2bpp/48B 改 1bpp/24B 后字位翻倍到 0xACE，不扩表则任何 >0x566 的码在 `0x80049834 lw s0,table[code]` 处**越界读**，拿到垃圾节点（乱 U/V）→ 从 VRAM 随机位置取图 → 花屏噪点（§9-3）。只需扩这张表；节点池（147 个）和 VRAM 格子**不用也不能**扩——节点和格子一一对应。

### #2 `0x8005B2E4`：对话字宽固定 12（流星原作）

```asm
addiu   a3,$zero,0xC
```
原指令 `lbu a3,0(v1)`，其中 `v1 = 0x800F2E80 + 码`（比例字宽表，仅 0x567 项）。中文统一 12px 宽，直接常量化，顺带消除高码越界。后面原有的 `bnez a3`（0 宽回退 0xE）变成永真分支，无害。

### #3 `0x8004A064`：菜单字宽固定 12（第三阶段补）

与 #2 **逐字节相同的读取序列在菜单渲染路径里还有一份**——这是 MIPS 编译器内联的结果，同一张表、同一段逻辑、两个地址。只改 #2 时对话正常、菜单里高码字读到垃圾宽度（"没"=129px）被拉成长白条并把整行推乱（§9-4）。修法与 #2 完全一致。**教训：找到一处硬编码，要搜整个二进制里它的同款。**

### #4 `0x8005C1D0`：字形步长 48 → 24（流星原作）

```asm
sll     r2,0x3          ; 原为 sll v0,v0,4
```
上文 `sll v0,a0,1; addu v0,v0,a0` 已算出 `码×3`；原版再 `<<4` 得 ×48（2bpp 每字 48B），补丁改 `<<3` 得 ×24（1bpp 每字 24B）。一条指令完成寻址制式切换。

### #5 `0x8005C218`：对话解码器 1bpp 展开（流星原作）

```asm
lbu  a0,0(t0)          ; 读 1 字节 = 8 个 1bpp 像素
andi v1,a0,0x1         ; 取 bit0
beq  v1,0,tz1
sll  v1,0x2            ; ★延迟槽：无论跳不跳都执行
tz1: ...               ; bit1..bit7 同理，各自移位后 or 进 v1
sw   v1,0(a1)          ; 写出 1 个 word = 8 个 4bpp 像素
...循环 0x18=24 次...
b    0x8005C2E4        ; 跳回原流程
```
逐位展开：bit i 置位 → `1<<i` 经 `sll 0x2` 变 4、再移到第 i 个 nibble ⇒ **置位(字体里=透明背景)→nibble 4，清零(笔画)→nibble 0**。巧妙处在利用 MIPS **分支延迟槽**：`beq` 后那条 `sll` 恒执行，bit=0 时移的是 0 结果仍是 0，省掉一半分支。24 字节输入 → 96 字节（16×12 4bpp）输出。对话调色板：0=文字色、4=透明。

### #6 `0x8004ACB8`：legacy 菜单解码器 1bpp 重写（第二阶段）

```asm
andi a0,a0,0xFFFF      ; 码取 16 位
sll  v0,a0,1
addu v0,v0,a0          ; ×3
sll  v0,v0,0x3         ; ×24
lui  t0,0x8011
lw   t0,-0x4B30(t0)    ; [0x8010B4D0] = F13 解压基址
addu t0,t0,v0          ; 字形源地址
legacy_f13_1bpp_loop:
lbu  a0,0(t0)
lui  v1,0x2222
ori  v1,v1,0x2222      ; 底色：8 个 nibble 全部预置 2
andi v0,a0,0x01
or   v1,v1,v0          ; bit0 置位 → nibble0 变 3
...bit1..7: sll 3/6/9/…/21 后 or...
sw   v1,0(a1)
...循环 24 次...
jr   ra
```
与 #5 同构但**输出约定不同**：底=2、置位=3（对应菜单 CLUT 0x7E10：idx2=亮字、idx3=透明）。另一个差异：**原版此函数先把 48 字节拷到 scratchpad（0x1F800000）再展开**，补丁直接从主存读，省去拷贝。
两解码器输出编码不同（0/4 vs 2/3）+ 共用一张字形缓存 ⇒ 曾出现"对话预缓存的字在菜单里发暗"（§9-5），靠改 F13 尾部 CLUT idx0=亮色收敛。**若未来重构，建议统一两者输出编码，消掉这层耦合。**

### #7 `0x8005C218` 之外的旁支：`.pool` 清理

流星的 asm 里误留过 ARM 专用的 `.pool` 指示符，armips 的 PSX 模式会报错——`executable_patch.py` 的 `normalize_asm` 在汇编前自动剔除。

### #8 `0x8006C27C`：队伍名字渲染器切换（第三阶段，Codex）

```asm
jal     0x800475FC     ; 原为 jal 0x80046FEC
```
唯一一条"改调用目标"型补丁。原目标 `0x80046FEC`：把名字的 16 位字库码**反查成 Shift-JIS**，再查 `[[0x80107378]+0x2448]` 的 16-bank 小字纹理表（覆盖 <0x200、实际填到 0x108）绘制 ~8px 小字——中文码全部落到共用回退图块（"由美"变".."）。新目标 `0x800475FC` 与其**参数约定完全相同**（a0-a3+栈参），但按 16 位码直读 F14 静态字库，因此一条 `jal` 即完成切换。
**配套前提**（缺一不可，都在构建系统里保证）：① 队伍可见名字（预设名+恶魔名译文）用字全部 <0x567（重排工具钉入）；② F14 也应用 original_ui_glyphs（主角假名名字在保留码位）。
代价：名字 12px、宽度走静态宽度表（11px 间距），比原版小字大一号（§12）。
