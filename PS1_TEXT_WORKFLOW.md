# 从屏幕文字到 PS1 光盘：本项目的实用学习路线

这不是一篇从 CPU 指令表开始背诵的 PS1 教程。目标是先让你能独立完成一次：

> 看到漏翻 → 找到记录 → 修改译文 → 验证 → 构建镜像 → 判断故障属于哪一层

做过几次以后，再往下看地址、MIPS 和 GPU 包，就不会只剩下一堆抽象名词。

---

## 1. 第一次自己修一条漏翻

### 第一步：用屏幕上连续的几个字反查

在 PowerShell 中：

```powershell
Set-Location 'P:\ROMHacking\SMT IF\new'
python tools/locate_text.py "落下雷电"
```

示例结果：

```text
F0094-00002D98-0000037E
编辑：data/text.json -> texts
资源：D/F0094.BIN，块 0x00002D98，指针槽 0x0000037E
原文：雷を落として敵１体を...
译文：落下雷电，使１个敌人...
```

只要屏幕上的文字在现有四套翻译数据里，工具会搜索原文、译文和记录 ID。
换行和普通空格不影响搜索，所以通常输入 3～8 个有辨识度的连续字符就够了。

其他实用命令：

```powershell
# 四类数据的翻译状态
python tools/locate_text.py stats

# 查看 overlay 中尚未改动的候选（默认只显示前 30 条）
python tools/locate_text.py todo --dataset overlay

# 只查正文资源
python tools/locate_text.py find "学校" --dataset text

# 已经知道记录 ID 时直接查
python tools/locate_text.py "F0094-00002D98-0000037E"
```

`todo` 把两种记录列为候选：

- `translation` 为空：构建时会回退到原文；
- `translation == source`：可能真的漏翻，也可能像“学校”一样中日写法本来相同，需人工确认。

### 第二步：只改 `translation`

在工具报告的 JSON 中搜索完整 `id`，只编辑这一条的 `translation`：

```json
{
  "id": "F0094-00002D98-0000037E",
  "block_offset": "00002D98",
  "pointer_offset": "0000037E",
  "source": "雷を落として敵１体を\nＳＨＯＣＫ状態にする",
  "translation": "落下雷电，使１个敌人\n进入触电状态"
}
```

暂时不要改：

- `id`、`block_offset`、`pointer_offset`；
- `offset`、`file_offset`、`max_bytes`；
- `renderer`；
- `{大停顿}`、`{主角}`、`{道具}`、`▽` 等控制标记。

这些字段不是说明文字，而是注入器定位原资源和选择渲染路径的依据。

### 第三步：先做无镜像验证

```powershell
$env:PYTHONPATH = '.;vendor'
python build.py --dry-run
```

它会实际完成文本编码、字库生成、资源重建和补丁断言，只是不写出 256 MB 光盘镜像。
如果译文用了码表中没有的新字，构建会明确报告“文本编码失败”，不要硬跳过。

### 第四步：生成测试镜像

```powershell
python build.py --output '..\Game\modified\SMT_IF_CN_test73_leak_fix.bin' --force
```

确认构建日志中 README 指定的关键行都出现，然后使用对应 `.cue` 冷启动。
改过码表或名字后，不要用旧即时存档恢复测试；即时存档会把旧 RAM 和旧字码一起恢复。

---

## 2. 屏幕上的文字究竟经过了什么

先记住这一条主链：

```text
光盘中的文件
  → 游戏把文件/资源读入 2 MB 主 RAM
  → CPU 读取文本字码
  → 字库解码器找到或生成字形
  → CPU 在 RAM 中组成 GPU 绘图包
  → DMA 把绘图包送给 GPU
  → GPU 从 VRAM 取字形并画到屏幕
```

我们做汉化时，并非只替换图片。我们同时改动链条中的三样东西：

1. 文本里的“字码”；
2. 字码对应的字模；
3. 某些原版渲染器无法容纳中文时的 MIPS 代码路径。

只改其中一个，画面就可能出现固定错字、空白、方块，甚至因绘图包写穿内存而卡死。

---

## 3. PS1 最少需要知道的硬件底层

### CPU 与内存

PS1 CPU 是小端序 MIPS R3000A。这个项目最常见的地址区：

| 区域 | 地址 | 意义 |
|---|---:|---|
| 主 RAM | 物理 `0x00000000–0x001FFFFF` | 只有 2 MB，代码、对象、文本、GPU 包都在这里挤 |
| KSEG0 | `0x80000000–0x801FFFFF` | 同一块 RAM 的缓存地址映射，反汇编里最常见 |
| KSEG1 | `0xA0000000–0xA01FFFFF` | 同一块 RAM 的不缓存映射 |
| Scratchpad | `0x1F800000–0x1F8003FF` | 1 KB 高速暂存区，游戏常放临时字符串 |
| VRAM | GPU 独立的 1 MB | 纹理、字形和画面，不是 CPU 普通 RAM 地址 |

`0x80046A18` 和物理 `0x00046A18` 指向同一块主 RAM。DuckStation 日志通常报告
CPU 使用的 KSEG0 地址。

PS1 是小端序。数值 `0x0123` 作为 16 位值写进文件时，字节顺序是：

```text
23 01
```

本作正文的一个字正好也是 16 位字码，所以查看十六进制时经常会看到“码位倒着存”。

### SLPM 文件偏移与运行地址

PS-X EXE 有 `0x800` 字节头部；本作正文载荷从 RAM `0x80010000` 开始。
因此对这个 SLPM：

```text
运行地址 ≈ 文件偏移 + 0x8000F800
文件偏移 ≈ 运行地址 - 0x8000F800
```

例如工具报告 SLPM 文件偏移 `0x000E4768`，就能推到运行时约
`0x800F3F68`。这就是“JSON 的 offset”和 DuckStation 断点地址之间的桥。

### MIPS 延迟槽

MIPS 的 `j`、`jal`、`jr`、条件分支后面那一条指令仍会执行，这叫延迟槽：

```asm
jal  0x800475FC
ori  a3,a3,1       ; 先执行这一条，才真正进入被调用函数
```

因此不能只把 `jal` 改到另一个渲染器就算完成；延迟槽可能正在准备参数，调用方还可能
通过栈传入绘图缓冲上限。我们之前多个“修一处、别处又坏”的根源就在这里。

### GPU 绘图包与 DMA 链

PS1 没有现代 UI 控件。CPU 会在主 RAM 中手工写一串 GPU 命令，例如一个 8×10 字符精灵，
再用链表 DMA 交给 GPU。每个节点的 tag 同时包含包长度和下一个节点地址。

如果渲染器实际生成的图元比调用方预留的空间多：

```text
正常： [文本对象][GPU 包缓冲][下一个对象]
溢出： [文本对象][GPU 包包包包包→覆盖下一个对象/下一条 DMA tag]
```

被覆盖的 tag 可能指向自己，于是 GPU 永远在同一节点循环，TTY 出现 `GPU timeout`；
也可能把 GPU 命令字 `0x04xxxxxx`、`0x78xxxxxx` 当成 CPU 指针，于是出现
`Invalid read`。这就是本项目曾经“文字显示了一点，随后整机像卡死”的底层原因。

---

## 4. 本作不是只有一套字库

| 路径 | 文本格式 | 主要用途 | 当前容量 |
|---|---|---|---:|
| F13 动态字库 | 16 位小端字码，`FFFF` 结束 | 对话、动态菜单 | `0xACE` |
| F14 静态字库 | 16 位小端字码，`FFFF` 结束 | 名字、技能、物品、固定 UI | `0x567` |
| F12 原版小字 | Shift-JIS 字节串，`00` 结束 | 少量原版系统 UI | 原版有限字集 |

这三条路径的“结束符”也不同。把 F13/F14 的 `FFFF` 规则套到小字串，或者把小字串的
NUL 规则套到正文，都会继续读过字符串末尾。

F14 的 `0x567` 不是随便写的限制，而是固定纹理排布能容纳的物理字形数。
本项目用“上下文别名”让不同 F14-only 界面复用低码格，但它只适用于已确认走 F14 的记录。
因此同一个汉字在普通对话与绿色技能说明里，构建时可能被编码成不同字码；这不是数据错乱，
而是为了在固定字库中装下更多字。

---

## 5. 四个翻译 JSON 分别是什么

| 数据 | 记录 ID 示例 | 最终去向 | 特点 |
|---|---|---|---|
| `text.json` | `F0094-00002D98-0000037E` | `D/F0094.BIN` | 大多数正文和资源文本 |
| `slpm_text.json` | `SLPM-000E4768` | `SLPM_871.54` | 可执行文件中的固定槽，保留 `renderer` |
| `overlay_text.json` | `F0049-00007D38` | `D/F0049.BIN` | 代码 overlay 内嵌文本，必须原位等长 |
| `f0098_text.json` | `F0098-00000800` | `D/F0098.BIN` | 种族/物品类别等裸表 |

以 `F0094-00002D98-0000037E` 为例：

- `F0094`：光盘资源文件；
- `00002D98`：文件里的资源块；
- `0000037E`：该块指针表中的旧字符串位置。

构建器不是把 JSON 整体塞进游戏。它读取这些定位字段，在干净原版资源上重建对应块，
修正允许修改的内部指针，再保证外层文件大小、固定块位置和光盘扇区约束仍成立。

---

## 6. 构建器实际做了什么

一次正常构建可按七层理解：

```text
1. 读取干净原版镜像和 extrac 基准
2. 从四个 JSON 选择 translation（空时回退 source）
3. 根据当前上下文把字符编码成 16 位字码/控制码
4. 生成 F13、F14 字模和上下文别名
5. 重建资源块，应用 SLPM、overlay、MIPS 补丁并核对原字节
6. 把等长文件写回原光盘扇区，重算 EDC/ECC
7. 回读验证；完整构建再写出 BIN/CUE
```

所以 `--dry-run` 很有价值：它验证第 2～5 层，而不必每次写完整镜像。
任何原位代码补丁都先断言原字节；基准不对时应立即失败，不能“差不多写进去”。

---

## 7. 看到不同症状时先查哪一层

| 屏幕/日志症状 | 优先怀疑 | 第一动作 |
|---|---|---|
| 清楚的日文原文 | 记录没有译文或漏掉数据源 | `locate_text.py "日文片段"` |
| 每次完全相同的错汉字 | 原版字码仍在用，但字库已重排 | `textscan.py rev "错汉字"` |
| 整句空白，只剩数字或少数字 | F14 字码超出 `0x567` / 未纳入别名上下文 | 先用 `locate_text.py` 找记录块 |
| 姓名显示成 `....` | 姓名走错渲染器或旧存档保存了旧字码 | 冷启动、新档复测 |
| 显示后越来越慢、GPU timeout | 图元超过缓冲或 DMA tag 被破坏 | 保存同帧 RAM/VRAM，查 GPU 链 |
| `Invalid read 0x04xxxxxx/0x78xxxxxx` | GPU 数据被当成 CPU 指针 | 记录 PC、坏地址和触发界面 |
| 立刻跳到 `0x80000080/84` | RAM 低端异常向量被覆盖 | 找更早的越界写，不要只补异常结果 |

如果 `locate_text.py` 找不到屏幕上的稳定错字：

```powershell
python tools/textscan.py rev "屏幕上的错字"
```

它会用当前码表把错字反查成字码。再用原版码表解释同一字码，才能知道游戏本来想显示
哪段日文。`locate_text.py` 解决“已提取记录在哪”，`textscan.py` 解决“尚未被提取的原始
字节在哪里”，两者用途不同。

---

## 8. DuckStation 调试时只记四样东西

出现不能用文本搜索解决的问题时，先保存：

1. 最小触发步骤；
2. 第一条异常日志的 `pc` 和非法地址；
3. 卡住时的寄存器（尤其 `pc`、`ra`、`a0-a3`、`s0-s7`）；
4. 同一帧的 2 MB RAM dump；涉及显示/DMA 时再保存 VRAM。

第一条异常通常比后面成千上万条更有价值。后续报错常常只是 RAM 已经被写坏后的连锁结果。

断点也应围绕“谁第一次产生坏值”设置：

- 非法读取发生在 `lbu/lw`：先看作为地址的基址寄存器是谁；
- 对该指针所在的对象字段设写入断点；
- 如果写入断点不触发，检查是否加载了对应测试镜像、是否用了旧即时存档、地址是否属于
  overlay（overlay 可能按场景换入同一 RAM 区）；
- 找到写坏字段的指令后，再向上追调用者和缓冲容量。

不要从最后的 GPU timeout 向后猜；它往往只是更早一次 CPU 越界写的结果。

---

## 9. 建议的练习顺序

### 练习 A：只做一条已有字符的漏翻

用 `locate_text.py` 找到一条短文本，只改 `translation`，跑 `--dry-run`，构建并实机确认。
这是最安全的完整闭环。

### 练习 B：根据 ID 理解资源层级

找同一个块的多条记录：

```powershell
python tools/locate_text.py "F0094-00002D98" --limit 10
```

你会看到它们都属于技能说明，只是指针槽不同。这样能直观看懂“文件 → 块 → 字符串”的层级。

### 练习 C：识别一条固定乱码

遇到每次相同的错汉字，用 `textscan.py rev` 得到字码，再用：

```powershell
python tools/textscan.py find "推测出的日文"
```

去原版资源搜索。此时你就在做最基础的 ROM 逆向：从显示结果反推编码，再从编码反推文件位置。

### 练习 D：读一小段 MIPS

先只回答四个问题：

1. 当前指令在读还是写？
2. 地址由哪个寄存器给出？
3. 这个寄存器从哪个调用参数/对象字段来？
4. 分支或调用的延迟槽做了什么？

不需要一开始就理解整段函数。能沿着一个坏指针向上追三步，已经足够解决很多实际问题。

---

## 10. 本项目的安全边界

1. 始终从 `Game/ogd/` 和 `extrac/` 的干净基准构建。
2. 不在旧测试镜像上叠补丁。
3. 不随意扩大/移动光盘文件、资源块或固定槽。
4. overlay 译文必须装进 `max_bytes`；短译优先。
5. 新增字符会影响全局码表和两套字库，必须全量重建并冷启动测试。
6. 不把“能编码为 Shift-JIS”当成“小字库能画出来”。
7. 不用旧即时存档验证改过的代码或码表。
8. 先保留能稳定复现的测试镜像、日志和 RAM dump，再试下一版。

当普通漏翻使用了码表中没有的汉字时，先停在 `--dry-run` 的明确报错处。新增字会牵涉
`src/codetable.py`、连续码位和 F14 容量，不再只是修改一句译文；等熟悉前面的闭环后再做。

